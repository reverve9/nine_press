# _legacy — 규칙 기반으로 옮기기 전의 작업물

`sclocalfood/_DEV/_docu/` 에 있던 것을 그대로 가져왔다. **참고용이고 빌드에 쓰지 않는다.**

| | |
|---|---|
| `html/` | 손으로 조판한 04·05면과 판면 견본. **판면 규칙 v2 의 출처** |
| `png/` | 그 대조 이미지 |
| `00_핸드오프_기획안실행계획서_v1_08171647.md` | 키노트 시절 이관문서 (프로젝트 문서와 같은 내용) |

지금 문안의 원본은 `content/sokcho/실행계획서.json` 하나다.
여기 있는 HTML 을 고쳐도 산출물은 바뀌지 않는다.
