# 🔄 HANDOFF · nine_press

> 마지막 업데이트: 2026-08-20 · 세션: Claude Code (개발)
> 현재 Phase: **P2 폭조절 완료 · P2.5-a 세로칸 완료** / 다음은 **P2.7-a · P2.5-b**
> 챗 세션 이관은 **`_DEV/00_이관_개발세션_v6.md` 가 최신.** 지면 현황 · 판정 · 함정은 그쪽이 정본이다.
> 이 문서는 **레포 · 코드 쪽** 정본이다.

---

## 0. 다음 세션이 첫 턴에 할 일

1. 이 문서를 끝까지 읽는다
2. **`_DEV/00_이관_개발세션_v6.md` §6 을 읽는다.** 미커밋분 정리가 최우선이고 갈래를 나눠 커밋한다
3. `_DEV/prompt/` 를 열어 **미착수 프롬프트**를 확인한다 (§3)
4. 착수 전 `git status --short` 로 작업 트리를 확인한다. **챗 세션과 동시 작업이 잦다**

---

## 1. 작업 방식 · 이 레포의 규칙

**구현은 프롬프트로 온다.** 사용자가 `_DEV/prompt/<단계>_<주제>_구현프롬프트.md` 를 써 두면 그것을 **그대로** 수행한다.

| | 규칙 |
|---|---|
| ① | **프롬프트 범위 밖을 건드리지 않는다.** 각 문서 §2 금지 사항이 우선한다 |
| ② | **결함을 발견해도 고치지 않는다.** 지시대로 넣고 §6 핸드오프에 **보고**한다. 사용자가 보정 프롬프트(`-b` · `-e`)를 새로 준다 |
| ③ | **`git` 을 직접 쓰지 않는다.** `sh scripts/save.sh "메시지" <경로들>` 만 쓴다 |
| ④ | **`next build` 를 돌리지 않는다** |
| ⑤ | **dev 서버(`localhost:4400`)에 붙지 않는다.** 눈 검사는 사용자가 한다 |
| ⑥ | 각 프롬프트 §5 관문을 전부 통과해야 커밋한다. 못 넘기면 **보고하고 멈춘다** |
| ⑦ | 표기 · `—` 를 쓰지 않는다. 구분자는 `·` 와 `\|`. 분량은 「쪽」으로 센다 |

**이 왕복이 실제로 다섯 번 돌았다.** P1 → P1-b · P2 → P2-b · P2-d → P2-e. 보고한 진단이 다섯 번 다 채택됐다.

---

## 2. 오늘(08-19 ~ 08-20) 커밋 · 개발 세션분

| 커밋 | 내용 |
|---|---|
| `4c08cda` | **P0-b** 수치 블록 편집 경계를 `.bv` 로 분리 · 단위 `.bu` 편집 개방 |
| `2d9f7c5` | **P1** 12칸 트랙 전환 · 폭 규칙을 `.row.g` 에서 전 행으로 |
| `b575c5c` | **P1-b** 트랙 계수 `92.73975` 정밀화 · 폭 기본값 누락 · `save.sh` 무인자 경로 |
| `04c8c3a` | **P2** 경계 끌기로 열 폭 조절 · 칸 합 12 강제 |
| `2f275bd` | **P2-b** 끌기 감도 · iframe 내부 좌표는 이미 판면 px |
| `ea107bd` `158164e` | **P2-c** 미리보기를 메모리 기준 `srcdoc` 으로 · `docs.js` 정리 |
| `8716e62` | **P2-d** 12칸 격자 토글 · 문서 교체 시 판본 지연 |
| `aa7a9f9` | **P2-e** 걸음 값 보정 · 격자와 끌기 스냅을 트랙에 맞춤 |
| `980ec09` | **P2-f** 격자를 페이지 전면으로 · 여백과 행 경계선 |
| `3961fa5` | **P2-g** 하한을 3칸으로 · 4열 구성 개방 |
| `6891700` | **P2.5-a** 세로 12칸 · 행 높이 선택적 고정 |

챗 세션분 `b14927a` · `b3127c9` · `21f8d28` 은 v6 §2 참조.

### 2-1. 판면 트랙 · 지금 값

```
가로   폭(w) = (92.73975 · w − 14.879) · u          page.css ㉗
       걸음 92.73975u = 180.75px · 칸 폭 151.75px · 거터 14.879u = 29px
       .bd 폭 2139.99px = 12칸

세로   높이(n) = (100% − 319px) / 12 · n + (n−1) · 29px    page.css ㉘
       [data-h] 있는 행 · .foot 에만. 없으면 지금대로 자동
```

**`92.73975` 는 칸 폭이 아니라 걸음이다.** 거터는 산식이 `−14.879` 로 이미 덜어낸다. 이걸 오해해 P2 와 P2-d 가 두 번 틀렸다.

### 2-2. 도구 현황 · 08-20 기준

- **제자리 편집** 글자 클릭 → 타이핑 · `⌘B` · Enter 줄바꿈 · Esc 되돌림 · `⌘S` 저장
- **구조 편집** 면 · 행 · 열 · 블록 · 내용 덩이 추가 · 삭제 · 순서
- **열 폭** 경계 끌기(`.rz` 손잡이) + 칸 수 칩 `3 · 4 · 5 · 6 · 7 · 8 · 12`
  - 하한 **3칸** · 행 합은 **언제나 12** · 이웃 열이 차액을 받는다
  - 끄는 동안은 인라인 `--w` 만 바꾸고 놓을 때 한 번 그린다. 되돌리기 스택에 한 칸만 쌓인다
- **열넣기** 6칸 이상일 때 3칸을 뗀다 → `12 → 9+3 → 6+3+3 → 3+3+3+3`
- **격자** `⌘\` · 자홍. 12칸 세로 띠 · 여백 4선 · 행 경계선. 페이지 전면 · 상태 저장 안 함
- **되돌리기** `⌘Z` / `⌘⇧Z` (60단계)
- **넘침 표시** 저장 시 사이드패널
- **미리보기가 메모리 기준이다** (P2-c). `srcdoc` 에 `render()` 결과를 직접 넣는다. 서버 왕복 0

---

## 3. 미착수 · 대기

| | 무엇 | 상태 |
|---|---|---|
| **1** | **`_DEV/prompt/P2.7a_렌더러확장_구현프롬프트.md`** | **미착수.** 블록 제목 `.bt` · 인라인 형광 `==` · 배지 `[[]]` · 단계띠 `.tl` · 왕복 복원. 강릉 브리프가 여기 묶여 있다 |
| 2 | **P2.5-b 세로 조작** | 프롬프트 **미작성.** 챗 세션이 쓴다. 선결 물음 5건은 v6 §9-1 |
| 3 | P3 블록 이동 드래그 | 프롬프트 미작성 |
| 4 | 구조도 문구 5건 | 사용자 판정 대기. v6 §4 |
| 5 | 미커밋분 정리 · 원격 연결 | v6 §6 |

---

## 4. ⚠️ 지금 작업 트리가 더럽다

**08-20 시점에 챗 세션 산출이 미커밋으로 남아 있다.**

```
 M rules/page.css        ㉙ gn 변종 238줄 + ㉚ 그림 자리
 M render/index.js       자리 배열 셋째 칸 = 그림 경로
 M scripts/build.js      그림 base64 · 상대경로
 M app/ui/Shell.jsx      미리보기 경로 치환
 M content/sokcho/실행계획서.json   23쪽 → 17쪽 재편 (−433줄)
?? app/api/img/[...p]/route.js · assets/캡처/ · _DEV/ 문서 다수
```

**갈래를 나눠 커밋한다** · ㉮ gn 이관 / ㉯ 그림 경로(코드 5파일) / ㉰ 지면 17쪽 재편 / ㉱ `_DEV` 문서.
**㉰ 은 반드시 따로.** 되돌릴 확률이 가장 높다(v6 §5-1).

**커밋 전에 roundtrip 을 돌린다.**

---

## 5. 검사 명령 · 관문

```sh
# 왕복 · 제자리 편집이 문안을 깨뜨리는지 전수 검사
node scripts/roundtrip.mjs content/sokcho/실행계획서.json          # 검사 1040개 · 불일치 0개
node scripts/roundtrip.mjs content/sokcho/사업장운영시뮬레이션.json  # 검사  600개 · 불일치 0개

# 검사용 빌드
node scripts/build.js content/sokcho/실행계획서.json --link         # 23면 (17쪽 재편 뒤 값 다름)
node scripts/build.js content/_check/폭조절.json --link             # 3면

# 도구 표식이 산출물로 새는지
grep -c "gridov" out/html/폭조절.html        # 0
grep -c "data-h" out/html/실행계획서.html     # 0 (아직 높이 안 준 상태)
grep -c 'class="row g"' out/html/실행계획서.html   # 면 수 − 표지

# 저장
sh scripts/save.sh "메시지" <경로들>          # 경로를 주면 그것만. 안 주면 작업 트리 전체
```

### 5-1. JSX 구문 검사 · `node --check` 가 안 먹는다

`app/ui/Shell.jsx` 는 JSX 라 `node --check` 가 실패한다. **`esbuild` · `typescript` 가 설치돼 있지 않다.** Next 에 딸린 babel 파서를 쓴다.

```js
// scratchpad 에 .cjs 로 두고 node 로 돌린다
const b = require('<레포>/node_modules/next/dist/compiled/babel/bundle.js');
const parser = b.parser();          // b.parser 는 팩토리 함수다. b.parser.parse 가 아니다
parser.parse(src, { sourceType: 'module', plugins: ['jsx'] });
```

---

## 6. ⚠️ 이슈 / 주의사항

- **원격이 없다.** 커밋 15개가 외장 볼륨 하나에만 있다
  `git remote add origin git@github.com:reverve9/nine_press.git && git push -u origin main`
- **`device_bash` 로 git 을 돌리지 마라.** `.git/*.lock` 을 못 지워 다음 git 이 막힌다. `save.sh` 를 쓴다
- **macOS 파일명은 NFD 다.** `git diff -- content/sokcho/실행계획서.json` 은 pathspec 이 안 걸린다. **디렉터리로 건다**
- **`save.sh` 무인자 호출은 작업 트리 전체를 담는다.** P0-b 때 세션 시작 전부터 남아 있던 32개가 같이 실렸다. **경로를 준다**
- **`rules/page.css` 는 끝에만 덧붙인다.** 앞 블록을 고치지 않는다. 지금 ㉚ 까지 · 1028줄(미커밋 포함)
- **규칙이나 인라인 표기를 고치면 `roundtrip.mjs` 를 돌린다.** 어긋나면 제자리 편집이 문안을 깨뜨린다
- 챗 쪽 VM 에는 playwright 브라우저가 없다. `roundtrip.mjs` · `pdf.js` 는 사용자 Mac 터미널에서만 돈다

---

## 7. 📁 구조

```
rules/page.css      판면 규칙. 끝에 덧붙인다 (① … ㉗ 가로트랙 ㉘ 세로칸 ㉙ gn 변종 ㉚ 그림자리)
content/            문안의 유일 원본  sokcho/ · gangneung/ · _check/
render/             순수 함수 렌더러 (index.js · inline.js). node 의존 0 · 클라이언트에서도 돈다
scripts/            build · pdf · shot · roundtrip · save.sh · fonts.sh
app/                Next 도구 (dev 전용. next build 를 돌리지 않는다)
  api/css/[name]    미리보기 srcdoc 이 <link> 로 거는 규칙. no-store
  api/font/[name]   assets/fonts 를 그대로. public/ 복사본을 두지 않는다
  api/img/[...p]    assets/캡처 를 그대로 (미커밋)
  api/doc           문안 읽기 · 쓰기
  ui/Shell.jsx      도구 전부. 제자리 편집 · 구조 편집 · 끌기 · 격자
  lib/docs.js       content 접근. loadDoc · saveDoc · docMtime · docPath · listDocs
assets/             fonts/ · 캡처/
out/                산출물. gitignore
_legacy/            규칙 이전 손조판본. 참고용
_DEV/prompt/        구현 프롬프트. 다음 세션이 수행할 원본
_DEV/00_이관_*.md    챗 세션 이관 문서. v6 이 최신
_DEV/_ref/          단계별 백업
```

**`api/preview` 는 P2-c 에서 지웠다.** 미리보기는 `srcdoc` 이다.

---

## 8. 💡 메모

- **`92.73975` 는 걸음이다.** 칸 폭은 `(92.73975 − 14.879) · u`. 두 번 틀린 자리다
- **iframe 내부 `clientX` 는 이미 판면 px 이다.** 바깥 `<iframe>` 요소에 `scale()` 이 걸려 있어도 브라우저가 히트테스트에서 변환을 역으로 통과시킨다. 나누지 않는다
- **`판본` useMemo 는 `문서ref.current` 를 읽는다.** ref 를 `setDoc` 보다 **먼저** 써야 그 렌더에서 잡힌다. `바꾸기()` 와 `불러오기()` 둘 다 그렇게 돼 있다
- **`[data-h]` 속성 셀렉터로 거른다.** `var(--h)` 만으로 거르면 값이 없을 때 선언 전체가 무효가 되어 `flex` 가 초기값으로 떨어진다
- **저장은 통째로 쓴다.** patch 를 만들지 않는다. 수정 시각 대조가 충돌을 막는다
- **어휘** 설명체 라벨을 쓰지 않는다. 명사구로 끝낸다
