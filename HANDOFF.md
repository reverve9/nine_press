# 🔄 HANDOFF — nine_press

> 마지막 업데이트: 2026-08-18 · 세션: claude.ai (챗)
> 현재 Phase: 1 / 2 — 블록 폼(텍스트 편집)

## 📍 현재 상태

판면 규칙 v4 (①~⑯) 확정. 실행계획서 P1 여덟 면(01~08) 작성 완료, 전 면 넘침 0 · 여유 29px.
도구에 **텍스트 편집 전용 블록 폼**을 붙였다. 구조 변경(행·열·블록 추가/삭제)은 여전히 JSON 직접.

## ✅ 완료

- **판면 규칙 v4** — `rules/page.css` 끝 ①~⑯
  - ①~⑤ §7 판정(볼드 낮춤 · 표 숫자 강조 해제 · 묶음 이름 보통 · 라벨 검정 · `.bt` 폐기)
  - ⑥ 인쇄 경로 교정 — `@media print` 뒤 무조건 규칙이 덮어 A4 좌상단에만 찍히던 결함
  - ⑦ 실무 확인 띠를 본문과 통일 · ⑧ 헤더 대시 −9.2u
  - ⑨⑩ 인쇄 농도(헤더 C / 띠 D) · 글자 먹
  - ⑪ 번호목록 배지 복구 · ⑫ `.row.g` 에서 폭 2·3 살리기 · ⑭ 배지 세로 정렬 — 전부 버그
  - ⑬ 표 `fit` + 밀도 `md` — 표가 블록 남는 높이를 행에 분배
  - ⑮ 목록 행 간격 52→60px · ⑯ 블록 테두리 `#ADADAD`
- **문안** — `content/sokcho/실행계획서.json` 01~08
  01 전체 타임테이블 / 02 접수·심사·선정 / 03 유형 배분 / 04 사전분석 / 05 진단 체계 /
  06 회차 구성 / 07 회차 운영 상세 / 08 회차 간 밀착 지원
- **PDF 경로 검증 완료** — 2340×1654 → A4 가로 297×210mm 벡터, 폰트 서브셋 내장
- **블록 폼 Phase 1** — 아래 파일 넷

## 🔧 진행 중 (미완료)

- **블록 폼 실물 확인 안 됨.** 챗 세션은 Mac 의 `localhost:4400` 에 못 붙는다.
  `npm run dev` 재시작 후 화면에서 확인해야 한다 — 서버 액션을 새로 export 했으므로 **재시작 권장**

## 📋 TODO

1. 블록 폼 동작 확인 — 글 고치기 · 줄 추가·삭제 · 저장 · 넘침 표시
2. 08면 내용 보강 (블록이 덜 찼다) · 02면 열 폭 다듬기
3. P2 면(09~11 · 16~19) 작성
4. 블록 폼 Phase 2 — 판면에서 블록 클릭 → 해당 폼으로 점프, 되돌리기
5. `git init` + 최초 커밋 (아직 저장소가 아니다)

## ⚠️ 이슈 / 주의사항

- **저장 충돌** — 화면 저장은 파일 수정 시각을 대조한다. 챗 세션이 `device_bash` 로 같은 JSON 을
  고친 뒤라면 저장이 **거부**된다. 문서를 다시 고른 뒤 이어서 고친다
- **면의 마지막 행에는 `g` 가 붙는다** — 폭 2·3 은 ⑫ 이후로 살아 있지만, 새 구성을 넣을 땐 확인할 것
- `device_bash` 는 네트워크가 없다 — `npm i` · `npx playwright install` 은 사용자 터미널에서만
- 크롬 `⌘P` 실물 확인은 사용자만 가능 (배경 그래픽 켜기 · 여백 없음 · A4 가로)

## 📁 최근 변경 파일

| 파일 | 변경 내용 | 상태 |
|------|----------|------|
| `rules/page.css` | 확정 v4 ①~⑯ | 완료 |
| `content/sokcho/실행계획서.json` | 01~08면 | 완료 |
| `render/index.js` | `.bt` 폐기 · `--w` 전달 | 완료 |
| `render/inline.js` | `{→05}` → `→ p.05` | 완료 |
| `app/lib/docs.js` | `docPath` · `docMtime` · `saveDoc` | 완료 |
| `app/actions.js` | `문안저장` · `문안불러오기` | 완료 |
| `app/ui/Shell.jsx` | 편집 모드 · 줄 추가·삭제 · 넘침 표시 | **미검증** |
| `app/globals.css` | 폼 · 넘침 배지 스타일 | **미검증** |
| `_DEV/_ref/Shell_before_form.jsx` | 폼 이전 원본 | 백업 |
| `_DEV/_ref/v3_backup/` | v4 이전 스냅샷 | 백업 |

## 💡 메모

- **행간 결정** — 남는 높이를 끝까지 분배(안 B)해 보니 항목 서넛짜리 블록에서 위아래로 흩어져
  오히려 안 읽혔다. 넉넉한 고정 행간 + 하단 여백(안 C)으로 확정
- **면 순서** — 02 모집 계획을 폐기하고 유형 배분으로 갈아끼운 뒤, 시간 순서에 맞춰 02↔03 교환
- **편집 저장은 통째로 쓴다** — patch 를 만들지 않고 화면이 든 doc 을 그대로 쓴다.
  수정 시각 대조가 충돌을 막으므로 patch 기계가 필요 없다
