# 🔄 HANDOFF · nine_press

> 마지막 갱신 2026-08-31 · 개발 세션
> **레포 · 코드 쪽 정본이다.** 지면 현황 · 판정 이력은 `_DEV/00_이관_개발세션_v8.md`.
> 원격 있음 · `git@github.com:reverve9/nine_press.git` · main 이 최신이다.

---

## 0. 첫 턴에 할 일

1. 이 문서를 끝까지 읽는다
2. `git log --oneline -12` 로 어디까지 왔는지 본다 · 작업 트리는 깨끗한 상태로 넘긴다
3. §6 이 다음 세션 착수점이다

---

## 1. 이 레포의 규칙

| | |
|---|---|
| ① | **`git` 을 직접 쓰지 않는다.** `sh scripts/save.sh "메시지" <경로들>` 만 쓴다 · 경로를 준다 |
| ② | **`next build` 를 돌리지 않는다** |
| ③ | **dev 서버(`localhost:4400`)에 붙지 않는다.** 눈 검사는 사용자가 한다 |
| ④ | **`rules/page.css` 는 끝에만 덧붙인다.** 앞 블록을 안 고친다 · `@media print` 는 언제나 맨 끝 |
| ⑤ | 판면·표기를 고치면 §4 관문을 전부 돌린다 |
| ⑥ | 표기 · `—` 를 쓰지 않는다. 구분자는 `·` 와 `\|`. 분량은 「쪽」으로 센다 |
| ⑦ | 설명체 라벨·안내문을 늘리지 않는다. 명사구로 끝낸다 |

---

## 2. 체계 요약 · 문안이 받는 열쇠

```
면    { 번호 · 제목 · 모드(카피\|연속) · 카피{메인,서브} · 논지 · 골격 · 자리[] }
골격  G1 전면 · G2 좌우 둘 · G3 3열 · G4 좌1:우2 · G5 상1:하2 · G6 4열 · G7 2×2
      G8 상3열:하전면 · G9 3열·박스 둘 · G10 3열 2·1·2 · G11 2:1 · G12 1:2
      **자리 개수가 골격이 만든 영역 수와 다르면 렌더러가 던진다**

자리  { 내용[] · 도형 · 비움 · 여백(안쪽 패딩) · 이름 }
      **자리 안은 배열이다** · 순서 자유 · 중복 자유 · 요소마다 제 도형
요소  { 열쇠 하나 + 도형(선택) }
      제목 · 요약 · 문단 · 목록[] · 번호목록[] · 표 · 수치[] · 그림 · 출처
      여백(42×n · 조판상의 틈)   비움(n \| [n,"무엇으로 채울지"] · 키노트로 넘긴다)
      도형{배경·투명도·테두리·굵기·모서리·그림자·글자:"반전"}   자리에도 요소에도 같은 어휘
      **옛 꼴도 읽는다** · `내용` 이 없으면 옛 열쇠를 고정 순서로 접는다 · 표식 경로도 옛 것
      둘을 같이 쓰면 던진다
      **이름을 나눠 쓰는 열쇠 둘** · 자리에선 여백=패딩 · 비움=자리 통째 · 요소에선 빈 칸 · 그 높이만
      **단계띠는 폐기했다** · 옛 열쇠를 만나면 던진다 · 그 높이를 「비움」으로 남기고 키노트가 그린다

간격  렌더러가 정해 클래스로 준다 · g0(0) · gh(머리뒤) · gb(문단사이) · page.css ㉝
      옛 조합표(`.bt + .bd`)는 요소가 자유로우면 못 버티고 · 인라인 margin 에 언제나 진다

표    머리[] · 행[[]] · 선(가로\|격자\|없음) · 칠{머리,행[]}
      폭    [1,2,1] 몫  \|  ["25%","30%","45%"] 백분율     열쇠 없으면 균등
      높이  "채움"  \|  [1,2,1]  \|  ["30%","70%"]  \|  [1,1,{"공백":0}]   없으면 한 줄 42

단계띠 칸[[머리,내용]] · 현재 · 칠 · 현재칠 · 모서리 · 글자/현재글자:"반전"

그림  "assets/캡처/….svg"  짧은 꼴  \|  {경로 · 높이 · 맞춤 · 설명}
      높이  "채움"(기본)  \|  블록 수 12 (42×12)  \|  "50%"      42 격자에 스냅
      맞춤  "전체"(기본 · 다 보인다)  \|  "채우기"(채우고 자른다)
      **그림은 배경이 아니다.** 제 자리를 먹는다 · 경로는 `assets/` 로 시작하는 상대경로만
```

**나머지는 언제나 마지막이 받는다.** `split()` · `rows()` · 표 폭 · 표 높이 전부 그렇다.
백분율은 합을 안 따진다 — 마지막 값은 안 읽고 `100 − 앞합` 으로 정한다.
**예외 하나** · 표 높이의 42 덩어리 나머지는 「공백」이 받는다. 공백이 없으면 위아래로 가른다.
마지막 행이 받게 하면 그 행만 84px 두꺼워져 표가 기운다(실측).

---

## 3. 도구 · 화면 짜임

```
상단바   문안 · ↺↻ · 기준선/외곽선 · 배율 · 넘침 · 빌드/폰트/PDF · 저장
왼쪽     면 미리보기 · 면마다 iframe 하나 · ↑↓ + 복제 −
가운데   판
오른쪽   속성 도크 · 고른 자리 + 비움 · 내용 목록(↑↓−) · 놓기 칩
         탭 [자리][요소]  ·  요소 탭이 고른 요소의 갈래를 보고 갈라진다
```

- **한 번 누르면 자리와 요소를 고르고 · 두 번 누르면 글자로 들어간다.** Esc 로 되돌리고 나온다
- **놓기는 박스에서 한다.** 자리마다 `+` 가 뜨고(고르거나 마우스를 얹으면) 누르면 **그 박스 위에** 놓기 판이 뜬다
  도크에서 놓으면 「어느 박스에?」가 늘 흐렸다 · 누른 박스가 곧 놓을 자리라 흐릴 것이 없다
- 도크 그림 견본은 **판으로 끌어다 놓을 수도 있다**
- 옛 꼴 자리는 「배열로」 를 누르거나 무엇이든 하나 놓으면 그때 접힌다
- 미리보기는 `render` 가 결정적이라 **안 바뀐 면은 같은 문자열** → 그 iframe 은 다시 안 뜬다
- `⌘Z`/`⌘⇧Z` 60단계 · `⌘S` 저장 · `⌘\` 기준선 자
- 저장은 통째로 쓴다. 수정 시각 대조가 충돌을 막는다

---

## 4. 관문

```sh
node scripts/기준선.mjs   <문안>     덩이 top · 높이가 42 배수인가
node scripts/자리재기.mjs <문안>     자리 안쪽 원점 기준 · 진짜 좌표 · 넘쳤는가  (playwright)
node scripts/roundtrip.mjs <문안>    제자리 편집이 문안을 깨뜨리는가          (playwright)
node scripts/비움.mjs   <문안>       비운 자리 · 요소 비움의 키노트 좌표표
node scripts/build.js <문안> --link  검사용 빌드
sh   scripts/save.sh "메시지" <경로>  커밋 + 푸시
```

**JSX 구문 검사** · `node --check` 가 JSX 를 못 읽는다. Next 에 딸린 babel 파서를 쓴다.

```js
const b = require('<레포>/node_modules/next/dist/compiled/babel/bundle.js');
b.parser().parse(src, { sourceType: 'module', plugins: ['jsx'] });   // b.parser 는 팩토리다
```

**08-31 기준 통과값**

```
자유배치.json  기준선 0/39   자리재기 0/39   roundtrip  60건 0   ← 배열 꼴
수치비움.json  기준선 0/27   자리재기 0/27   roundtrip  53건 0   ← 요소 비움
표.json      기준선 0/81   자리재기 0/81   roundtrip 297건 0
그림.json     기준선 0/29   자리재기 0/29   roundtrip  36건 0
도형 · 목록 · 위계 · 비움 · 판면기준            기준선 · 자리재기 · roundtrip 전부 0
실행계획서 --v3                              roundtrip 1012건 0
```

**옛 문안 여덟이 배열 전환 뒤에도 값이 한 픽셀도 안 바뀌었다.** 그게 간격 규칙이 맞다는 증거다 ·
`자유배치.json` 03면은 같은 내용을 옛 꼴과 새 꼴로 나란히 두어 눈으로도 대조한다.

- `판면기준.json` 자리재기 **7건 넘침** · 글이 자리보다 긴 것이다. 회귀 기준 문안이라 안 건드린다
- `폭조절.json` 과 속초·강릉 문안 넷은 **옛 12칸 트랙**이다. 새 렌더러가 못 읽는다 · `--v3` 봉인본으로만 그린다

---

## 5. ⚠ 함정 · 실측으로 잡은 것

- **인라인 `margin-top` 은 CSS 위계 간격을 덮어쓴다.** 표 채움이 머리뒤 42 를 먹어 표가 42px 올라앉았다. `기준선.mjs` 는 84 도 126 도 42 배수라 못 잡는다 → `자리재기.mjs` 가 잡는다
- **채움은 뒤도 봐야 한다.** 출처(.lb)가 42 를 먹는다. 안 빼면 출처가 자리 밖으로 밀린다
- **표 칸이 자리를 덮는다.** 그래서 「한 번 고르기 · 두 번 글자」가 필요했다
- **`.dbg` 는 검사용 외곽선이고 문안에 안 남는다.** `doc.판면` 은 폐기 · 옛 열쇠는 던진다
- **iframe 안 `clientX` 는 이미 판면 px 이다.** 바깥 scale 을 나누지 않는다
- **macOS 파일명은 NFD 다.** `git diff -- content/…/실행계획서.json` 이 안 걸린다 · 디렉터리로 건다
- **`판본` useMemo 는 `문서ref.current` 를 읽는다.** ref 를 `setDoc` 보다 먼저 써야 그 렌더에서 잡힌다
- **짧은 꼴 열쇠에는 표식 번호를 안 붙인다.** `"문단": "한 줄"` 에 `["문단",0]` 을 주면 편집기가 문자열의 첫 글자에 쓴다. `roundtrip` 이 잡았고 고쳤다 · 배열일 때만 번호를 단다
- **「여백」이 두 뜻이다.** `자리.여백` 은 안쪽 패딩 · `내용[].여백` 은 42 덩이 빈 칸이다. 접을 때 같이 지우면 자리 패딩이 날아간다 · `자리내용열쇠` 가 그래서 있다
- **백분율 높이는 앞의 줄 수를 알아야 한다.** 옛 코드는 안 세고 그렸다 — 그림 `"50%"` 가 제목·요약만 보고 남은 높이를 잡아 실제보다 컸다. 배열 모델의 확정높이() 가 잡았다
- **「비움」도 두 뜻이다.** `자리.비움` 은 자리를 통째로 넘기고 `내용[].비움` 은 그 높이만 넘긴다. `자리겹침` 이 겹침 검사에서 둘을 뺀다
- **요소 비움의 y 는 앞이 전부 확정일 때만 적는다.** 앞에 문단 · 목록이 있으면 줄 수를 못 세니 `?` 로 두고 이유를 댄다. 어림한 좌표를 주면 키노트에서 그대로 어긋난다 · 실측 대조 7건 전부 일치
- **도구 표식은 자에서 뺀다.** `.emp` · `.plus` 는 흐름 밖이라 `자리재기.mjs` 가 건너뛴다
- 챗 쪽 VM 에는 playwright 브라우저가 없다 · `roundtrip` · `자리재기` · `pdf` 는 사용자 Mac 에서만 돈다

---

## 6. 다음 세션 착수점

> ~~③ 그림~~ · ~~② 도형의 걸리는 범위~~ 는 **끝났다** · `N-그림` · `N-자유`.
> ② 는 **㉮(요소마다 도형)** 로 갔다 — 자유 좌표로 얹는 ㉯ 가 아니다.
> 자리 안이 배열이 되면서 「흐름 안에서 요소마다 배경」이 자연스러운 답이 되었다.

### ① 글자 편집 · 하나 남았다

지금 도크에는 **자리 · 요소 속성만** 있다. 글자는 판면에서 고치는 것뿐이고 **계층 · 크기를 못 고친다.**
재료는 모여 있다 · 설계 §8-1 · v8 §9 ②.

- **행간은 잠근다**(42 격자). 크기는 계단으로 · 하한 21
- 실물 PDF 실측 · 본문 21·22·23 · 라벨 20 · 페이지타이틀 40 · 강조 50 · 카피 65
  우리 본문은 24 다 · 판정 「PDF 보다 작으면 안 되지만 크면 상관없다」 → **24 유지**
- 도크 `[요소]` 탭에 크기 계단을 얹는다. **자리가 아니라 요소에 건다** — 배열이 그 길을 냈다

### ② 얹는 층 · 아직 안 냈다

배열은 **흐름**이다. 흐름 밖에 놓이는 것은 여전히 없다.

- **a2 주석의 「선 얹기」** · 골격과 무관하게 판에 얹는 선
- **도형 안 배경 이미지** · 지금 그림은 제 자리를 먹는 블록이지 도형의 배경이 아니다
- 자리 안 상대 좌표 · 42 격자를 받아야 한다

**다만 급하지 않다** · 흐름 밖의 것은 요소 「비움」으로 자리를 남겨 키노트가 맡는다.
단계띠를 그렇게 넘겼다 — 앱이 다 하려 들지 않는 것이 이 도구의 방침이다 · 설계 §5-11.

`비움` 은 「앱이 못 하는 자리를 키노트로 넘긴다」는 계약이라 **그림과 자유 배치가 들어와 비움이 줄었다** —
v8 §9 ③ 의 「빈 자리와 비움을 계속 가를지」를 `N-이관` 에서 다시 센다.

### ③ Finder 에서 그림 끌어다 놓기

지금은 `assets/` 에 **미리 넣어 둔** 그림만 쓴다. 도크 견본을 판으로 끌 수는 있어도
Finder 에서 바로 못 끈다 — **그림을 쓰는 길이 레포에 없다.** `api/img` 는 읽기뿐이다.
정할 것 · 쓰는 자리(`assets/캡처/`?) · 이름 겹침 · 크기 상한 · NFD 파일명 ·
**`assets/` 가 git 에 들어간다**(실물 39쪽어치면 무시 못 할 값이다).

---

## 7. 미뤄 둔 것 · 알고 있는 구멍

- **오류판에 떨어지면 편집기에서 못 고친다.** 배열이 되며 「그 요소만 비워 그리기」가 값싸졌다 · 렌더러가 던지면 면 전체가 오류판이 되어 자리를 못 고른다 → JSON 을 손으로 고쳐야 한다. 「문안을 열 때 검사해 그 자리만 비워 그리기」가 값싼 개선안이다
- **값 하나 바꿀 때마다 판 iframe 이 통째로 새로 뜬다.** 판이 한 번 깜빡인다 · 다시 안 그리고 DOM 만 손대는 길을 따로 내야 한다
- **면이 많은 문안에서 미리보기 로딩**은 아직 눈으로 확인 안 됐다. 무거우면 썸네일 자체 캐시로 바꾼다
- **N-이관** · 실물 39쪽을 새 체계로 옮기는 일이 통째로 남아 있다. 표 50건이 여기서 실전에 오른다
- 9열 표 1건은 예외로 남겨 뒀다

---

## 8. 📁 구조

```
rules/page.css      판면 규칙. 끝에만 덧붙인다 (… ㉜ 그림 · ㉝ 간격 · ㉞ 도구 표시 · @media print 는 맨 끝)
                    `.sp`(단계띠)는 아무도 안 부르는 죽은 규칙이다 · 규칙을 통째로 손볼 때 걷는다
content/            문안의 유일 원본  sokcho/ · gangneung/ · _check/
render/             순수 함수 렌더러 (index.js · inline.js) · node 의존 0
scripts/            build · pdf · roundtrip · 기준선 · 자리재기 · 비움 · save.sh · fonts.sh
app/                Next 도구 (dev 전용)
  api/css · api/font · api/doc
  api/img           `/api/img` 목록 · `/api/img/<경로>` 파일 하나
  ui/Shell.jsx      도구 전부 · 미리보기 · 제자리 편집 · 속성 도크
  lib/docs.js       content 접근
_DEV/               이관 문서 · 설계 · 프롬프트
_legacy/            규칙 이전 손조판본
```
