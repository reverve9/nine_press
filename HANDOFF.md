# 🔄 HANDOFF · nine_press

> 마지막 갱신 2026-08-31 · 개발 세션 · `N-도크재편` · 앞은 `N-얹기 a~e` · `T0` · `T1`
> **도크가 탭 둘([판][내용])로 섰다.** 다음은 §6.
> **레포 · 코드 쪽 정본이다.** 지면 현황 · 판정 이력은 `_DEV/00_이관_개발세션_v8.md`.
> 원격 있음 · `git@github.com:reverve9/nine_press.git` · main 이 최신이다.

---

## 0. 첫 턴에 할 일

1. 이 문서를 끝까지 읽는다
2. `git log --oneline -12` 로 어디까지 왔는지 본다 · 작업 트리는 깨끗한 상태로 넘긴다
3. **§6 이 다음 세션 착수점이다** · 겹침 검사 · 판면 구성 · 라벨 둘
4. 도크가 어떻게 서 있는지는 §3 이 정본이다 · **탭은 [판] · [내용] 둘뿐이다**

**지난 세션이 낸 것** · 커밋 일곱 · 전부 main 에 있다
```
N-그림        그림은 배경이 아니다 · 제 박스를 먹고 42 격자에 스냅한다
N-자유 a      박스 안은 배열이다 · 순서 자유 · 중복 자유 · 요소마다 제 도형
N-자유 b      단계띠 폐기 · 그 높이는 요소 「비움」으로 남겨 키노트가 맡는다
N-자유 c      탭을 [박스][요소] 둘로 · 놓기는 박스 위에서 한다
N-자유 d      Finder 에서 바로 끌어다 놓는다 · assets/올린것 에 쓴다
N-자유 e      말을 일반어로 · 면→페이지 · 자리→박스 · 골격→레이아웃 · 논지→요지 …
N-글자 c      계층 갈아타기 · 크기 계단 · 굵게 · **계획의 마지막 기능이 닫혔다**
N-글자 d      구간 스타일 · 드래그한 몇 글자에만 걸린다 · 도크 범위가 둘로 갈렸다
N-글자 e      글자 색도 도형과 같은 꼴로 · 견본 + 최근색 + #RRGGBB · 표기는 data-i 에 싣는다
N-그림 b~e    옛 꼴 박스도 배열로 읽는다 · 고른 요소에 테 · 견본 접기 · 실치수 표시
N-얹기 a~d    **흐름 밖에 놓이는 층** · 판 전역 절대 좌표 · 선 · 도형 · 그림 · 끌기 · 층 · 과녁
```
**`N-글자 c` 가 도크에서 깨진 것 넷을 같이 고쳤다** · 아래 §6. 셋은 지난 세션이 낸 것이다 —
**사용자가 아직 눈으로 다 안 봤다** 는 상태에서 나왔고 · 이번에 코드를 훑어 잡았다.

---

## 1. 이 레포의 규칙

| | |
|---|---|
| ① | **`git` 을 직접 쓰지 않는다.** `sh scripts/save.sh "메시지" <경로들>` 만 쓴다 · 경로를 준다 |
| ② | **`next build` 를 돌리지 않는다** |
| ③ | **dev 서버(`localhost:4400`)에 붙지 않는다.** 눈 검사는 사용자가 한다 |
| ④ | **`rules/page.css` 는 끝에만 덧붙인다.** 앞 블록을 안 고친다 · `@media print` 는 언제나 맨 끝 |
| ⑤ | 판면·표기를 고치면 §4 관문을 전부 돌린다 |
| ⑥ | 표기 · `—` 를 쓰지 않는다. 구분자는 `·` 와 `\|`. 분량은 「쪽」으로 센다 |
| ⑦ | 설명체 라벨·안내문을 늘리지 않는다. 명사구로 끝낸다 |
| ⑧ | **말은 일반어로 쓴다** · N-자유 e. 레포만 쓰던 말을 걷었다 · §2 표를 따른다 |

---

## 2. 체계 요약 · 문안이 받는 열쇠

**말을 바꿨다** · `N-자유 e` · 전수조사. 옛 열쇠를 만나면 렌더러가 무엇으로 바뀌었는지 대고 던진다.

```
면 → 페이지    자리 → 박스    골격 → 레이아웃    논지 → 요지
표.머리 → 표.헤더            표.칠 → 표.배경 (칠.머리 → 배경.헤더)
박스.여백 → 박스.안여백       요소.여백 → 요소.빈칸
폭 · 높이의 「몫」 → 「칸수」   (말만 바뀌었다 · 값의 꼴은 그대로다)
scripts/자리재기.mjs → scripts/박스재기.mjs
```

**안 바꾼 것** · `비움` 은 「앱이 안 하고 키노트로 넘긴다」는 계약을 담은 말이라 대체어가 흐리다.
**봉인본(`--v3`)은 옛 말 그대로다** — 영구 보존이라 안 고친다. `roundtrip` · `build` 가 거기서만 갈라 읽는다.

```
페이지    { 번호 · 제목 · 모드(카피\|연속) · 카피{메인,서브} · 요지 · 레이아웃 · 박스[] }
레이아웃  §2-1 표 · **박스 개수가 영역 수와 다르면 렌더러가 던진다**

박스  { 내용[] · 도형 · 비움 · 여백(안쪽 패딩) · 이름 }
      **박스 안은 배열이다** · 순서 자유 · 중복 자유 · 요소마다 제 도형
요소  { 열쇠 하나 + 도형(선택) + 크기(선택) }
      제목 · 요약 · 문단 · 목록[] · 번호목록[] · 표 · 수치[] · 그림 · 출처
      여백(42×n · 조판상의 틈)   비움(n \| [n,"무엇으로 채울지"] · 키노트로 넘긴다)
      도형{배경·투명도·테두리·굵기·모서리·그림자·글자:"반전"}   박스에도 요소에도 같은 어휘
      **크기** 21 · 24 · 26 · 29 계단 넷 · 글자 요소에만 · 자유 숫자를 주면 던진다 · N-글자 c
      **행간은 안 받는다** · 42 에 잠긴 채다 · 크기만 갈면 격자가 안 깨지고 줄 수도 안 변한다
      **옛 꼴도 읽는다** · `내용` 이 없으면 옛 열쇠를 고정 순서로 접는다 · 표식 경로도 옛 것
      도구도 같은 순서로 가상으로 접어 읽는다 · `Shell.jsx 박스접기()` ↔ `내용읽기()`
      둘을 같이 쓰면 던진다
      **이름을 나눠 쓰는 열쇠 둘** · 박스에선 여백=패딩 · 비움=박스 통째 · 요소에선 빈 칸 · 그 높이만
      **단계띠는 폐기했다** · 옛 열쇠를 만나면 던진다 · 그 높이를 「비움」으로 남기고 키노트가 그린다

얹기  **흐름 밖이다** · 페이지에 붙는다 · `페이지.얹기[]` · 판 전역 절대 좌표 2339 × 1654
      `{ "선": "가로\|세로" · x · y · 길이 · 굵기(1~20) · 색 }`
      `{ "도형": {…} · x · y · 폭 · 높이 }`   도형 어휘는 박스 · 요소와 같다
      `{ "그림": "assets/…" \| {경로·맞춤·설명} · x · y · 폭 · 높이 }`
      **얹기 그림은 42 스냅이 없다** · 내보낸 크기 그대로 앉는다 · 그래프처럼 비율이
      못박힌 것에 쓴다. **흐름 안 그림을 대신하지 않는다** — 「제목 → 그림 → 문단」과
      「채움」이 그것 위에 선다 · 둘 다 산다
      **층** `"뒤"`(기본) · `"앞"` · 박스보다 먼저 나오냐 나중에 나오냐 · 쌓임은 DOM 순서가 전부다
      앞에 두어도 **누르는 것은 안 가로챈다** · `pointer-events` 가 꺼져 있다
      **층은 거르기 전에 검사한다** — 거른 뒤에 보면 모르는 값이 앞에도 뒤에도 안 걸려 사라진다
      **세로를 42 에 안 붙인다** · 글을 안 담아 기준선과 무관하다 · 거터 한가운데(21)가 잦다
      기준선 · 박스재기가 안 잰다 — 흐름 밖이라 격자 검사가 건너뛴다
      옛 열쇠 `구분선` · `선` 을 만나면 「이제 얹기다」로 던진다

박스  **제 자리를 들 수 있다** · N-자유배치 · 사용자 판정
      `{ x · y · 폭 · 높이 · 내용[] · 도형 · 비움 · 안여백 · 이름 }`   **얹기와 같은 어휘다**
      넷을 다 주거나 하나도 안 준다 · 한 박스가 들면 그 페이지 박스가 전부 들어야 한다
      자리를 들면 렌더러가 **레이아웃을 안 본다** · `레이아웃` 은 어느 견본에서 찍었나의 기록
      **안은 그대로 흐른다** · 42 격자 · 확정높이() · 넘침 · 간격 · 계층 전부 그대로다
      실측 · `content/_check/자유배치시험.json` 기준선 0/6 · 박스재기 0/6 · roundtrip 10건 0

안내선 **작업 보조다** · 문서에 하나 · 모든 페이지에 같이 뜬다 · N-자 · 사용자 판정
      `문서.안내선 { "가로": [y…] · "세로": [x…] }`   가로줄은 y · 세로줄은 x 다
      **렌더러가 안 읽는다** · 모르는 열쇠라 조용히 지나간다 · 판에 안 샌다(실측)
      그리는 자리도 판 밖(`.frame`)이라 `srcdoc` · PDF · 산출 HTML 에 안 들어간다
      `문서.기준선: true` 와 같은 갈래다 — 작업 보조인데 문안에 남는다.
      반대로 `.dbg` 외곽선은 안 남긴다 · 그때그때 켜 보는 것이라서다

구간  **드래그한 몇 글자에만 걸린다** · 요소 속성이 아니라 문자열 안 표기다 · N-글자 d
      `{결론|38억원}`   `{강조·29|세 축}`   `{결론|**38억**}`  ← 겹치기 `·` · 중첩 된다
      색   먹 · 네이비 · 강조 · 결론 · 부연 · 출처  **또는 `#RRGGBB`** · 도형과 같은 규칙이다
           이름은 이름대로 둔다 — 문안에서 읽히고 · 색을 갈면 따라온다 · `#` 은 곁길
      크기  21 · 24 · 26 · 29        굵기  400 · 700 · 800      자간  좁게 · 보통 · 넓게
      네 표는 이름이 안 겹쳐 접두 없이 섞어 쓴다 · 모르는 토큰이면 던진다
      **한 갈래에 토큰 하나** · 도크가 갈아 끼운다 · 같은 칩을 다시 누르면 벗긴다
      「민글로」가 구간 표기와 `**굵게**` 를 **함께** 벗긴다 · 층은 둘인데 쓰는 쪽엔 하나다
      **표기는 `data-i` 에 문자열 그대로 실린다** · 칠은 클래스(이름)와 인라인 style(`#`)로
      갈리는데 되읽기는 `data-i` 하나만 본다 — 순서가 저절로 맞고 `#` 도 실린다
      옛 표기 그대로다 · `**굵게**`(700+강조색) · `{TBD}` · `{TBD협의}` · `{→05}`

간격  렌더러가 정해 클래스로 준다 · g0(0) · gh(머리뒤) · gb(문단사이) · page.css ㉝
      옛 조합표(`.bt + .bd`)는 요소가 자유로우면 못 버티고 · 인라인 margin 에 언제나 진다

표    헤더[] · 행[[]] · 선(가로\|격자\|없음) · 배경{헤더,행[]}
      폭    [1,2,1] 칸수  \|  ["25%","30%","45%"] 백분율     열쇠 없으면 균등
      높이  "채움"  \|  [1,2,1]  \|  ["30%","70%"]  \|  [1,1,{"공백":0}]   없으면 한 줄 42

단계띠 칸[[머리,내용]] · 현재 · 배경 · 현재칠 · 모서리 · 글자/현재글자:"반전"

그림  "assets/캡처/….svg"  짧은 꼴  \|  {경로 · 높이 · 맞춤 · 설명}
      높이  "채움"(기본)  \|  블록 수 12 (42×12)  \|  "50%"      42 격자에 스냅
      맞춤  "전체"(기본 · 다 보인다)  \|  "채우기"(채우고 자른다)
      **그림은 배경이 아니다.** 제 박스를 먹는다 · 경로는 `assets/` 로 시작하는 상대경로만
```

### 2-1. 페이지 레이아웃 · **실측**

`render/index.js 영역()` 을 돌려 뽑았다 · 안여백 33 기준 · **박스 안쪽 치수**다.
그림을 자르거나 원고 분량을 잴 때 쓰는 숫자가 이것이다.

```
레이아웃        박스  카피 모드                        연속 모드
G1  전면         1   2113 × 909                     2113 × 1108
G2  좌우 둘      2   1002 × 909                     1002 × 1108
G3  3열          3   632 · 631 × 909                632 · 631 × 1108
G4  좌1:우2      3   1002 × 909  ·  1002 × 395·405  1002 × 1108 · 479·520
G5  상1:하2      3   2113 × 227  ·  1002 × 573      2113 × 311  · 1002 × 688
G6  4열          4   447 · 446 × 909                447 · 446 × 1108
G7  2×2          4   1002 × 395·405                 1002 × 479·520
G8  상3열:하전면  4   632·631 × 227 · 2113 × 573     632·631 × 311 · 2113 × 688
G9  3열 2단      6   632·631 × 395·405              632·631 × 479·520
G10 3열 2·1·2    5   632 × 395·405 · 631 × 909      632 × 479·520 · 631 × 1108
G11 2:1          2   1372 × 909 · 632 × 909         1372 · 632 × 1108
G12 1:2          2   632 × 909 · 1372 × 909         632 · 1372 × 1108
```

**모드가 세로 예산을 정한다** · 카피 909 · 연속 1108 · **연속이 22% 더 든다.**
카피 존 156 을 카피 모드만 쓰기 때문이다 · 가로는 모드와 무관하다.

**1px 씩 갈리는 열이 있다**(632 · 631 · 447 · 446). `split()` 이 나머지를 마지막에 주기
때문이고 의도한 것이다 — 합이 언제나 판폭에 딱 맞는다. **평균으로 뭉개 적지 마라.**

**나머지는 언제나 마지막이 받는다.** `split()` · `rows()` · 표 폭 · 표 높이 전부 그렇다.
백분율은 합을 안 따진다 — 마지막 값은 안 읽고 `100 − 앞합` 으로 정한다.
**예외 하나** · 표 높이의 42 덩어리 나머지는 「공백」이 받는다. 공백이 없으면 위아래로 가른다.
마지막 행이 받게 하면 그 행만 84px 두꺼워져 표가 기운다(실측).

---

## 3. 도구 · 화면 짜임

```
상단바   이름 · ↺↻ · 기준선/외곽선 · 배율 · 넘침 · 빌드/폰트/PDF · 저장
왼쪽     문안 고르기 · 페이지 미리보기 · 페이지마다 iframe 하나 · ↑↓ + 복제 −
가운데   판
오른쪽   속성 도크 · **탭 둘이 맨 위다** · 머리띠가 없다 · N-도크재편
         [판]     판면(이름 · 카피 영역 · 격자 열둘) → 박스(N / M · 비움) → 도형 일곱
         [내용]   레이어 → 삽입(스위치 + 갈래) → 고른 것
```

**도크 머리띠(`.dkhd`)를 걷었다** · 사용자 지적 · N-도크재편 b. 「G1 · 박스 1/1 · 비움」이
맨 위 고정 띠에 앉아 있었는데 · **아무것도 안 골랐을 때 빈 채로 46px 를 먹어** 두 탭 위에
여백만 남겼다. 세 값이 각자 제자리로 내려갔다.

```
G1(=페이지.레이아웃)   → [판] 맨 위 「판면  G6 · 4열 · 4칸」   판면을 고르는 자리다
박스 N / M            → [판] 「박스」 줄 · 도형 바로 위       박스 꼴을 만지는 자리다
                        [내용] 층 목록 칸막이가 또 말한다     「박스 1 / 4 · 3개」
비움                  → [판] 「박스」 줄 · 「무엇으로 채울지」가 그 아래
```

**「G1」 배지는 되살리지 마라.** 그것은 `페이지.레이아웃` 열쇠를 그대로 낸 것이고 ·
판면 격자에 이름표를 안 붙이기로 한 결정(도식이 곧 이름이다)의 짝이라 · **지금 것 하나만**
[판] 탭 머리에 적는다. 내용을 만지는 동안에는 안 나온다.

**탭이 둘이다** · N-도크재편 · 사용자 판정. 넷(판면 · 박스 · 요소 · 얹기)일 때는
**두 무리가 섞여 있었다** — 판면과 박스는 그릇이고(박스가 몇 개 생기나 · 어떻게 생겼나) ·
놓기와 얹기는 내용이다(무엇을 담나). 층이 다른 넷이 한 줄에 나란히 서 있었다.

```
그릇   판면(골격) + 박스(꼴)      →  [판]
내용   놓기(요소) + 얹기          →  [내용]
```

**놓기와 얹기는 대등하다** · 사용자 판정. 둘 다 「내용을 하나 더 놓는 일」이고
다른 것은 **자리를 누가 정하느냐 하나다** — 놓기는 흐름이 정하고(순서대로 쌓인다 ·
앞이 길어지면 뒤가 밀린다) · 얹기는 내가 정한다(판 절대 좌표 · 안 밀린다).
그 하나뿐인 차이가 **「삽입 · 박스 / 페이지」 스위치**로 선다 · 갈래 목록도 · 고른 것 판도
· **판이 누구를 받는지도** 이 스위치가 정한다(옛 [얹기] 탭이 하던 일이다 · N-얹기 d).
그림이 양쪽에 다 있는데 전에는 두 탭에 갈라져 같은 이름으로 섰다 —
이제 「그림 · 박스」 · 「그림 · 페이지」로 한 자리에 선다.

**두 값이 데이터를 그대로 가리킨다** — `박스.내용[]` 과 `페이지.얹기[]` 다.
```
[내용] 순서   레이어 → 삽입 → 고른 것
고른 것       박스   : 요소(↑↓−) · 계층·크기 → 갈래별(비움·표·그림) → 구간 → 도형
              페이지 : 갈래 · 레이어 · 자리 → 갈래별(글·선·그림·도형) → − 얹기
```

**라벨 규칙** · 사용자 판정 · N-라벨. 규칙 ⑦ 의 연장이고 · 전수로 훑었다.

```
① 명사로 끝낸다        `~하기` · `~기` 꼴을 안 쓴다
② 설명을 안 단다        곁말 · title · 안내문에 「무엇을 한다」를 안 적는다
                     곁말은 **값만** 낸다 — `1 / 4` · `447 × 1108px` · `G6 · 4열 · 4칸`
③ 열쇠를 그대로 쓴다     라벨이 문안 열쇠와 같으면 그 말을 쓴다
```

**포토샵 · 키노트에 설명이 없다** · 사용자 지적. 혼자 쓰는 앱에 구구절절이 붙어 있었다.

```
층 → 레이어        놓기 → 삽입        박스 안 → 박스        판 위 → 페이지
카피 영역 [있다][없다] → 모드 [카피][연속]      ← 열쇠가 `모드` 다 · 있다/없다는 값도 아니었다
계층 → 갈래         얹기 판의 「갈래」와 같은 물건이라 이름도 같다
민글로 → 민글       바꾸기 → (폐기)       무엇 · 무엇으로 채울지 → 채울 것
− 요소 · − 표 · − 그림 · − 비움 · − 얹기 → **삭제**   줄 이름이 이미 무엇인지 말한다
닫기 → ×           채우기 → 채움(값은 `채우기` 그대로다)
```
**문안 열쇠는 안 바뀐다** · `얹기[].층` · `모드` · `맞춤:"채우기"` 그대로다 —
열쇠는 계약이고 라벨은 읽는 말이다.

**걷은 것**
```
그림 견본 갤러리      같은 그림을 문서 안에서 두 번 쓸 일이 없어 값을 못 했다 · 사용자 판정
                   자리마다 「파일」 칸 하나다 — 고르는 것이 곧 놓는 것이다
                   `/api/img` GET · `견본판` · `그림목록` · `견본` 상태를 다 걷었다
                   Finder 에서 판의 박스로 끌어다 놓는 길은 그대로 산다
곁말 · title 설명    「끌어서 옮긴다 · Shift 자석」 「값은 그대로 나른다」 「다 보인다」 …
```

**부품** · 키노트 캡처를 **기본 참조**로 잡았다 · 사용자 판정.

```
스테퍼      수칸 오른쪽에 ⌃⌄ 가 붙는다 · 범위 글자(0~2339)는 걷었다 · title 로만 남는다
팝업 메뉴    다섯이 넘는 목록은 칩 줄이 아니라 메뉴다 · 삽입 갈래 열하나 · 요소 갈래 여섯
            **둘 ~ 넷은 선택띠 그대로다** · 키노트가 그렇게 가른다
묶음 접기    「▾ 판면」 처럼 삼각형 + 이름 · 누르면 접힌다 · 곁말이 머리에 붙는다
            판면 · 박스 · 도형 · 고른 글자 · 요소 도형 다섯 · 접은 상태는 브라우저에만 남는다
구분선      가장자리까지 안 긋는다 · 판 안으로 들인다(`margin: 14px 0`)
```
`[내용]` 도크 내용 높이 1441 → **1391** · 삽입 칸이 세 줄에서 한 줄로(196 → 84px).

**바탕 톤** · 중성 회색으로 갔다 · 캡처의 인스펙터가 그렇다.
```
--pn  #eceff3 → #ececec        --pln #d2d2d2 (패널 안 테두리 · 구분선)
선택띠 트랙 #e0e0e0 · 켠 칸 흰 알약 + 그림자
```

**글자 색** · 키노트 인스펙터 캡처와 대조했다 · `_DEV/스크린샷 …11.24.52.png`.
**이름표는 회색이 아니라 진한 먹이고 · 왼쪽 정렬이다** — 「서체」 「간격」 「단락 앞」이 다 그렇다.
회색은 **못 쓰는 것**과 **곁말**에만 쓴다(꺼진 칸의 「없음」 · 범위 숫자 · 맛보기).

```
.fldnm  회색 · 오른쪽 정렬  →  --onx(#2b3644) · 왼쪽 정렬 · 칸 64px
.chip · .barin 글자        --tx → --onx        칸 안 글자도 먹이다
--pn    #f2f4f7 → #eceff3  판과 흰 칸의 대비를 키운다
활성 탭                     **현행 유지** · 사용자 지시 · 키노트의 파란 알약을 안 쓴다
```

**바닥 색** · **패널은 회색 · 칸은 흰색** · 키노트 결 · 사용자 판정.
흰 판에 흰 칸을 얹으면 어디가 만질 수 있는 자리인지 테두리 하나로만 갈렸다.

```
--pn #f2f4f7   패널 바닥(.pages · .dock)      판(--bg #eef0f3)이 제일 어둡다
#fbfcfd        상단바                         칸 · 카드 · 썸네일은 흰색이다
.seg           회색 트랙 + **켠 칸이 흰 알약**   맥 세그먼티드 컨트롤 결
.chip.on       #dee5ec                       회색 바닥에서 흰 칩과 갈리려면 더 진해야 한다
.tab.on        밑줄과 글자색만                 흰 판을 안 깐다 · 아래가 회색이라 안 이어진다
```

**칸 꼴** · **이름표가 왼쪽에 선다** · 키노트 인스펙터 결 · 사용자 판정.
빈자리 후반 작업이 키노트에서 나므로 **두 판이 같은 결이면 손이 안 갈아탄다.**

```
.fld   62px 이름표 | 값     이름표는 오른쪽 정렬 회색 · 곁말은 값 뒤에 붙는다(.fldx)
.fld.hd                   묶음 머리(판면 · 레이어)는 왼쪽에 붙고 칸을 안 나눈다
칸 사이 17 → 9px           칩 6/11 → 5/10 · 수칸 높이 30 → 27 · 구분선 18 → 14
```
**한 화면에 드는 줄이 늘었다** · 위아래로 쌓던 때보다 도크 내용 높이가 줄었다.
```
[판]    1653 → 1341px
[내용]  2060 → 1441px      선 하나 고른 상태는 899 로 **스크롤이 아예 없다**
```
**값 이름은 값 옆에 둔다** — 색 견본 뒤의 「없음」이 줄 끝(hex 칸 아래)에 떨어지면
무엇의 이름인지 안 읽힌다 · 실측으로 잡았다.

**꼴** · **껍데기에 볼드가 없다** · 사용자 요구 · 키노트 인스펙터 결이다.
볼드가 섞이면 이름표 쪽으로 눈이 끌려 판이 정신없어진다 · 사용자 지적.
**굵기로 위계를 만들지 않는다** — 이름표는 회색 · 값과 칩은 진한 회색 · 둘 다 400 이고 ·
켠 것은 판(`--on`)과 테(`--on2`)로만 말한다.

```
.fldnm  11.5 / 600 → 13.5 / 400 · 회색      곁말 11.5 → 12.5
.chip   12.5 / 600(on) → 13 / 400          .eln 12 / 600 → 13 / 400
.tab    13 / 500 · on 700 → 14 / 400        .laynm 10.5 / 700 → 12 / 400
.brand · .save · .chk · .znow · .pgno · .pgtt · .dp-hd · .numin · .lyrbx  전부 400
```
`.top`·`.side`·`.dock` 안 글자의 computed font-weight 를 세어 **400 하나만 남은 것을 확인했다.**
남은 볼드는 판 안 오류판(`.er h1` · `.er .m`)뿐이다 — 그건 껍데기가 아니다.

**박스 내용 일괄 삭제** · 사용자 요구. [판] 탭 「박스」 줄에 `내용 삭제` 가 선다 —
`비움`(키노트로 넘김)과 다른 물건이다. 지운 뒤 `내용: []` 이 남는데
`박스비움()` 이 **빈 배열을 내용으로 세고 있어** 지우고도 비움이 안 열렸다 · 고쳤다.

**레이어 목록** · N-도크재편 · **데이터는 안 바꿨다**. 내용이 대등해져 겹칠 수 있으니
무엇이 위인지 볼 자리가 들었다. 한 목록으로 내되 문안은 그대로다 —
`얹기[].층: '앞'|'뒤'` 와 **배열 순서** 둘뿐이다. 숫자 층을 새로 만들면 렌더러 계약이
바뀌어 봉인본 회귀가 깨진다.

```
레이어 · 위가 앞
  글    "축제앱을 새로 짓지 않는다"      얹기 · 층 앞
  도형                                얹기 · 층 앞
  ═══ 박스 1 / 4 · 3개 ═══             박스와 그 안 요소가 통째로 여기 든다
        제목 · 문단 · 목록              고르면 펼쳐진다 · 흐름 순서다 · 쌓임이 아니다
  선                                  얹기 · 층 뒤
  도형  배경                            얹기 · 층 뒤
```

- **같은 층에서는 배열 뒤가 위다.** 그래서 목록은 배열을 뒤집어 낸다
- **칸막이를 넘기면 층이 앞 ↔ 뒤로 바뀐다** · 넘어간 쪽에서는 칸막이에 가장 가까운
  자리에 선다(`unshift` · `push`) — 눈에 보이는 대로 앉는다
- 박스와 그 안 요소가 **통째로 한 칸**인 것은 렌더러가 얹기층('뒤') · 박스들 ·
  얹기층('앞') 순으로 내놓기 때문이다 · 그 사이를 가를 자리가 없다

### 3-0. 자유 배치 · N-자유배치

**모든 것을 얹기 개념으로 간다** · 사용자 판정. 키노트가 그렇다 — 슬라이드에는
자유롭게 놓인 개체만 있고 **글 상자 안만 흐른다.**

**상자 안의 흐름은 못 걷는다.** 그게 클로드가 y 를 안 정해도 되는 유일한 이유다 —
문단이 몇 줄로 감길지는 재야 나오고(`확정높이()` 가 `null` 을 준다) · 자유 좌표면
문단마다 y 를 클로드가 정해야 한다. 키노트 텍스트 상자도 똑같다.

**그래서 바뀐 것은 사각형이 어디서 오느냐 하나뿐이다.**

```
영역()      41행    「계산한다」 → 「있으면 읽고 없으면 계산한다」   ← 여기만 바뀌었다
블록() 94 · 확정높이() 28 · 내용읽기() 52 · 표그리기() 110 · 그 밖 전부   그대로
```
`renderPage` 가 `블록(박스[i], r, …)` 로 사각형을 넘겨 줄 뿐이라 · 블록은 그것이
레이아웃에서 왔는지 문안에서 왔는지 모르고 알 필요도 없다.

```
[판] 탭 「배치」  [판면] ↔ [자유]
자유로 넘기면     지금 판면의 자리를 박스에 찍는다 · 그 뒤엔 레이아웃을 안 본다
판면으로 되돌리면  열쇠 넷을 지운다 · 박스 수가 그 판면이 내는 수와 다르면 막고 이유를 댄다
판면 견본        자유일 때도 산다 · 고르면 그 자리로 **다시 찍는다**
                안 찍으면 판면을 골라도 판이 하나도 안 바뀌어 누르고도 아무 일이 없다
자리 · 크기       도크 수칸 넷 · 판에서 끌어서도 옮긴다 · Shift 자석
```

- **판에서 끌린다** · `.wrap.free` 일 때 `[data-박스]` 가 끌린다 · 얹은 것과 같은 길이다
- **글자로 들어간 뒤에는 안 끈다** — 그때 끄는 것은 글자 고르기다 ·
  `e.target.isContentEditable` 로 가른다. 한 번 눌러 고르고 두 번 눌러 들어가는 길은 그대로
- **세로 자석은 42 격자를 본다**(`자석('y', true)`) · 박스 안쪽 원점이 격자에 앉아야
  안 글이 기준선을 지킨다 · 기준선 관문이 그것을 잰다
- 「박스」 쪽을 볼 때만 끌린다 · 「페이지」 는 얹은 것 차례다

**잃는 것 하나** · 판면을 갈아도 자리를 든 박스는 안 움직인다.
그게 자유 배치의 값이자 대가다 — 그래서 판면 견본이 다시 찍어 주는 길을 냈다.

---

### 3-1. 자 · 안내선 · N-자

**자는 손잡이다** · 사용자 판정. 포토샵 · 키노트가 그렇듯 **자에서 끌어 내려야 안내선이 생긴다** —
자를 재는 도구로 쓰자는 것이 아니다. 값은 안내선에 있다.

```
자석이 아는 선   박스 모서리 · 거터 한가운데 · 42 기준선(글만)  ← 전부 판면이 파생시킨 것
                + **안내선**                                ← 사람이 세운다 · N-자
```

판면이 안 만든 자리에는 붙일 데가 없어 숫자를 손으로 쳤다. **자석 후보에 한 줄
더하는 것이 이 기능의 전부다.**

- 자 두 줄 · 위(x) · 왼쪽(y) · 21px · **판 밖에 산다**(`.stage`) · 좌표 × 축척
- 눈금은 화면에서 54px 보다 촘촘해지지 않는 계단을 고른다 · 가로 100 배수 · **세로 42 배수**
- 자에서 끌면 생기고 · 선을 끌면 옮겨지고 · **판 밖으로 끌면 지워진다**(자로 되끄는 것이 곧 그것이다)
- Shift 를 누르면 안내선도 자석에 붙는다 · 박스 모서리 · 거터 · 가로줄은 42 격자
- 상단바 「안내선」이 **자 · 선 · 자석을 한꺼번에 끈다** — 안 보이는 선이 잡아채면 이유를 못 댄다
- 줄은 두께가 0 이고 `:before` 가 11px 과녁을 만든다 · 1px 선은 축척 30% 에서 화면 0.3px 이다

**색이 역할을 가른다.**
```
주황 #E68100    박스 계열      고른 박스 · 고른 요소            판에서 「지금 고른 것」
보라 #7A4FD6    페이지 계열    얹은 것 · 점선 · 고른 것 진하게    N-자 · 사용자 요구
청록 #0DA5B8    안내선
분홍 (page.css) 기준선 42 격자
```
박스에 넣은 것과 페이지에 얹은 것이 둘 다 주황이라 **판만 보고는 어느 층 것인지 못 읽었다** ·
삽입 스위치로 둘이 대등해진 뒤로 더 그랬다 · 사용자 요구. `page.css` 의 `.ovp .ov` 규칙을
**도구가 판에 넣는 style 에서 덮는다** — 판면 규칙은 앞 블록을 안 고친다(규칙 ④) ·
고르기 표시 색이 한자리에 모이는 이득도 있다.

---

**도크의 판정 범위가 둘이다** · N-글자 d. 위는 요소에 걸리고 아래는 고른 글자에 걸린다.
```
요소 범위    계층 · 크기 · 도형        문단 하나가 최소 단위 · 문안의 요소 열쇠로 앉는다
구간 범위    색 · 크기 · 굵기 · 자간    판면에서 끌어 고른 몇 글자 · 문자열 안 표기로 앉는다
```

- **한 번 누르면 박스와 요소를 고르고 · 두 번 누르면 글자로 들어간다.** Esc 로 되돌리고 나온다
- **「페이지」면 얹은 것이 옅은 점선으로 드러난다.** 2px 선은 안 그러면 어디 있는지 못 찾는다
- **얹은 것은 판에서 끌어 옮긴다** · N-얹기 b. 끄는 동안은 인라인 style 만 만지고 · 놓을 때 x·y 를 **한 걸음으로** 문안에 앉힌다(따로 부르면 되돌리기가 두 걸음이 된다)
- **Shift 를 누르면 자석이 붙는다** · 박스 모서리 · 거터 한가운데 · 10px 안. 「박스 사이 선」이 이 층을 연 이유라 손으로 맞출 일을 없앴다 · 안 누르면 자유다
- **「페이지」면 박스가 누르기를 안 받는다** · N-얹기 d. 얹은 것이 박스보다 작아 그 안에 들면 박스가 위를 덮어 클릭을 통째로 가져갔다 · 사용자 지적. 「페이지」는 얹는 것만 만지는 모드다 — 보이는 순서는 그대로고 글자 편집은 「박스」에서 그대로 산다
- **얹기 그림은 견본을 고르는 것이 곧 놓는 것이다** · 고른 얹기가 있으면 그 자리를 갈아 끼운다 · 크기는 그림의 자연 비율을 재서 앉힌다
- **얹은 것은 「페이지」일 때만 판에서 눌린다** · N-얹기. 늘 열어 두면 판을 덮은 도형이 박스 고르기를 통째로 가로챈다 · `.wrap.ovp` 가 그때만 `pointer-events` 를 연다
- **고른 것에 테가 붙는다** · N-그림 d. 박스는 주황 3px · 요소를 고르면 **박스 테가 물러나고**
  그 요소가 진한 테를 받는다 · 강한 테는 언제나 하나다. `outline` 이라 42 격자에 안 닿는다
- **놓기는 박스에서 한다.** 박스마다 `+` 가 뜨고(고르거나 마우스를 얹으면) 누르면 **그 박스 위에** 놓기 판이 뜬다
  도크에서 놓으면 「어느 박스에?」가 늘 흐렸다 · 누른 박스가 곧 놓을 곳이라 흐릴 것이 없다.
  도크 [내용] 탭의 삽입 칩도 산다 — 그쪽은 **고른 박스**에 넣는다
- 도크 그림 견본은 **판으로 끌어다 놓을 수도 있다**
- **옛 꼴 박스도 배열처럼 읽는다** · N-그림 b. 내용 목록이 똑같이 뜨고 요소를 고를 수 있다 ·
  고르기만 할 때는 안 접고 · **손대는 순간 진짜로 접힌다**. 「배열로」 버튼은 폐기했다
- 미리보기는 `render` 가 결정적이라 **안 바뀐 페이지는 같은 문자열** → 그 iframe 은 다시 안 뜬다
- **구간 판은 글자를 담는 갈래에만 뜬다** · 글자 여섯 + 표 + 수치. 그림 · 빈칸 · 비움엔 안 뜬다
- **그림을 놓은 뒤에는 놓은 것 하나만 위에 남는다.** 견본은 아래 「견본 펴기」로 접혀 있다 · N-그림 c
  도크 「+ 그림」은 견본을 편다 — 접은 뒤로 아무것도 안 여는 것처럼 보였다
- **구간은 판면에서 끌어서 고른 뒤 도크 칩을 누른다.** 두 번 눌러 글자를 연 상태여야 한다
- **칩은 다 `mousedown` 을 막는다.** 안 막으면 누르는 순간 판면이 focus 를 잃고 고른 자리가 날아간다
- **계층은 열쇠만 갈아 끼운다** · 값 · 도형 · 크기가 그대로 따라간다 · 글 ↔ 목록은 줄바꿈으로 가른다
- **크기는 계단 넷뿐이다** · 행간은 안 딸려 온다 · 「기본」은 열쇠를 지우는 것이다
- `⌘Z`/`⌘⇧Z` 60단계 · `⌘S` 저장 · `⌘\` 기준선 자
- 저장은 통째로 쓴다. 수정 시각 대조가 충돌을 막는다

**껍데기 꼴** · `app/globals.css` · 판면 규칙과 안 섞인다 · N-꼴 a
```
글자    크기를 올리고 굵기를 내렸다 · 800/700 을 700/600/500 으로 · 10~12px 을 11~13px 로
활성    **네이비를 안 쓴다** · 연회색 판(--on · --on2 · --onx) 이다
        주황은 「지금 보는 페이지」와 저장에만 남겼다 · 그 둘은 상태가 아니라 자리다
폭      왼쪽 208 · 우패널 300 → 360 · 썸네일 174
자리    문안 고르기가 상단바에서 왼쪽 맨 위로 · 상단바 좌측은 이름만
```

---

## 4. 관문

```sh
node scripts/기준선.mjs   <문안>     덩이 top · 높이가 42 배수인가
node scripts/박스재기.mjs <문안>     박스 안쪽 원점 기준 · 진짜 좌표 · 넘쳤는가  (playwright)
node scripts/roundtrip.mjs <문안>    제자리 편집이 문안을 깨뜨리는가          (playwright)
node scripts/비움.mjs   <문안>       비운 박스 · 요소 비움의 키노트 좌표표
node scripts/build.js <문안> --link  검사용 빌드
sh   scripts/save.sh "메시지" <경로>  커밋 + 푸시
```

**JSX 구문 검사** · `node --check` 가 JSX 를 못 읽는다. Next 에 딸린 babel 파서를 쓴다.

```js
const b = require('<레포>/node_modules/next/dist/compiled/babel/bundle.js');
b.parser().parse(src, { sourceType: 'module', plugins: ['jsx'] });   // b.parser 는 팩토리다
```

**08-31 기준 통과값**

**문안 경로를 통째로 준다** · `node scripts/기준선.mjs content/_check/표.json`.
이름만 주면 열지 못하고 던진다.

```
자유배치.json  기준선 0/39   박스재기 0/39   roundtrip  60건 0   ← 배열 꼴
수치비움.json  기준선 0/27   박스재기 0/27   roundtrip  53건 0   ← 요소 비움
표.json      기준선 0/81   박스재기 0/81   roundtrip 297건 0
그림.json     기준선 0/29   박스재기 0/29   roundtrip  36건 0
글자크기.json  기준선 0/35   박스재기 0/35   roundtrip  54건 0   ← 크기 계단 · N-글자 c
얹기.json     기준선 0/29   박스재기 0/29   roundtrip  39건 0   ← 얹는 층 · N-얹기
구간표기.json  기준선 0/26   박스재기 0/26   roundtrip  38건 0   ← 구간 스타일 · N-글자 d
도형 0/107 · 목록 0/37 · 위계 0/45 · 비움 0/14 · 판면기준 0/205   roundtrip 전부 0
실행계획서 --v3 1012건 0 · 사업장운영시뮬레이션 600건 0 · 홍보전략브리프 295건 0 · 폭조절 94건 0
```

**옛 문안 여덟이 배열 전환 뒤에도 값이 한 픽셀도 안 바뀌었다.** 그게 간격 규칙이 맞다는 증거다 ·
`자유배치.json` 03페이지는 같은 내용을 옛 꼴과 새 꼴로 나란히 두어 눈으로도 대조한다.

- `판면기준.json` 박스재기 **7건 넘침** · 글이 박스보다 긴 것이다. 회귀 기준 문안이라 안 건드린다
- `폭조절.json` 과 속초·강릉 문안 넷은 **옛 12칸 트랙**이다. 새 렌더러가 못 읽는다 · `--v3` 봉인본으로만 그린다

---

## 5. ⚠ 함정 · 실측으로 잡은 것

- **인라인 `margin-top` 은 CSS 위계 간격을 덮어쓴다.** 표 채움이 머리뒤 42 를 먹어 표가 42px 올라앉았다. `기준선.mjs` 는 84 도 126 도 42 배수라 못 잡는다 → `박스재기.mjs` 가 잡는다
- **채움은 뒤도 봐야 한다.** 출처(.lb)가 42 를 먹는다. 안 빼면 출처가 박스 밖으로 밀린다
- **표 칸이 박스를 덮는다.** 그래서 「한 번 고르기 · 두 번 글자」가 필요했다
- **`.dbg` 는 검사용 외곽선이고 문안에 안 남는다.** `doc.판면` 은 폐기 · 옛 열쇠는 던진다
- **iframe 안 `clientX` 는 이미 판면 px 이다.** 바깥 scale 을 나누지 않는다
- **macOS 파일명은 NFD 다.** `git diff -- content/…/실행계획서.json` 이 안 걸린다 · 디렉터리로 건다
- **`판본` useMemo 는 `문서ref.current` 를 읽는다.** ref 를 `setDoc` 보다 먼저 써야 그 렌더에서 잡힌다
- **짧은 꼴 열쇠에는 표식 번호를 안 붙인다.** `"문단": "한 줄"` 에 `["문단",0]` 을 주면 편집기가 문자열의 첫 글자에 쓴다. `roundtrip` 이 잡았고 고쳤다 · 배열일 때만 번호를 단다
- **「여백」이 두 뜻이었다.** `박스.여백`(패딩)과 `내용[].여백`(빈 칸)이 같은 이름이라 접을 때 패딩이 날아갔다. `안여백` · `빈칸` 으로 갈라 놨다 · N-자유 e
- **백분율 높이는 앞의 줄 수를 알아야 한다.** 옛 코드는 안 세고 그렸다 — 그림 `"50%"` 가 제목·요약만 보고 남은 높이를 잡아 실제보다 컸다. 배열 모델의 확정높이() 가 잡았다
- **「비움」은 아직 두 뜻이다.** `박스.비움` 은 박스를 통째로 넘기고 `내용[].비움` 은 그 높이만 넘긴다. 뜻이 이어져 있어 일부러 안 갈랐다 · `박스겹침` 이 겹침 검사에서 뺀다
- **요소 비움의 y 는 앞이 전부 확정일 때만 적는다.** 앞에 문단 · 목록이 있으면 줄 수를 못 세니 `?` 로 두고 이유를 댄다. 어림한 좌표를 주면 키노트에서 그대로 어긋난다 · 실측 대조 7건 전부 일치
- **도구 표식은 자에서 뺀다.** `.emp` · `.plus` 는 흐름 밖이라 `박스재기.mjs` 가 건너뛴다
- **목록 항목은 크기를 안 물려받는다.** `.ls li` · `.ol li` 가 제 `font-size` 를 갖고 있어 `ul` 에 준 인라인 크기가 안 닿는다. 크기를 준 요소에만 `.fz` 를 붙이고 `page.css ㉟` 이 그 하나를 연다 · N-글자 c
- **크기를 열어도 행 상자는 안 자란다.** `line-height` 를 px 로 박아 두어서다 · 실측 : 21 · 24 · 26 · 29 넷 모두 본문·제목·요약·목록 42 · 출처 28. 그래서 `확정높이()` 의 42 가 그대로 맞는다. **42 를 넘는 계단(예 52)을 열면 `확정높이()` 도 같이 가야 한다**
- **구간 크기는 `line-height:0` 으로 앉힌다.** 한 줄에 크기가 섞이면 브라우저가 기준선을 맞추느라 상자들의 합집합을 잡고 그 값이 42 를 넘는다(실측 · 29px 를 섞으면 44 · 21px 도 43). 조각의 줄 상자 기여를 0 으로 만들면 부모의 42 스트럿만 남아 버틴다 · 감기는 문단에서도 42 배수 · 기준선은 그냥 씌운 것과 같은 자리다
- **`inline-block` 으로 못박으면 안 감긴다.** `height:42` 를 주면 42 는 지키는데 조각이 한 상자에 갇혀 글이 밖으로 흘러 겹친다(실측 · 조각수 1 · 두 문장이 42px 상자 안에). 격자만 보고 고르면 이걸 놓친다
- **`원문()` 사본이 둘이었다가 갈라졌다.** `Shell.jsx` 와 `scripts/roundtrip.mjs` 가 따로 들고 있었고 · 구간 표기를 `inline()` 에만 넣었더니 roundtrip 17건이 틀렸다. **`render/inline.js` 로 모았다** — `inline()` 의 역함수라 한 파일에 산다. roundtrip 은 브라우저 안으로 못 가져가니 `원문.toString()` 으로 넣는다
- **판에서 고른 것이 눈에 안 보였다.** 박스에만 테가 있고 요소에는 없었다 · 글자는 두 번 눌러 들어가면 편집 테가 뜨는데 **그림은 그 길이 없어** 무엇을 고른 것인지 판에서 못 읽었다 · 사용자 지적 · N-그림 d. 요소 테를 넣고 · 겹치지 않게 박스 테를 물러나게 했다. 판이 다시 뜨면 클래스가 날아가므로 `재기()`(onLoad) 도 같은 함수를 부른다
- **박스를 고르면 요소 고르기가 지워졌다.** `useEffect(… , [박스번호, …])` 가 요소를 풀었는데 · 판에서 그림을 누르면 클릭 하나가 박스와 요소를 같이 세운다 → 박스 바뀜을 보고 요소를 지워 **그 클릭이 통째로 무효**가 됐다 · N-그림 c. 글자는 두 번 눌러 들어가는 길이 따로 있어 안 드러났고 **그림만 드러났다** · 사용자 지적. 박스만 고른 클릭은 고르는 쪽이 이미 요소를 함께 푼다 — 효과에서 뺐다
- **옛 꼴 박스에서는 요소를 골라도 박스가 잡혔다.** `요소찾기()` 가 옛 꼴이면 박스 자신을 냈고 · `요소열쇠()` 가 첫 열쇠를 집어 **그림을 눌러도 갈래가 「제목」이었다** · 사용자 지적 · N-그림 b. `data-요소` 는 옛 꼴에도 제대로 붙어 있었다(번호도 `박스접기()` 와 맞았다) — **도크만 못 읽고 있었다**. 읽기를 가상 접기로 · 쓰기를 실제 접기로 갈랐다
- **판이 갈래를 안 보면 그림을 골라도 텍스트 판이 뜬다.** 구간 줄(고른 글자·색·크기·굵기·자간)을 갈래와 무관하게 [요소] 탭 맨 위에 뒀더니 그림을 고른 사람이 「요소를 못 고른다」고 읽었다 · 사용자 지적 · N-글자 e. **탭의 순서가 곧 무엇을 고쳤는지를 말한다** — 갈래별 판이 먼저 와야 한다
- **뒤 층은 박스가 덮는다.** DOM 상 박스보다 먼저 나오므로 · 얹은 것이 박스 안에 들면 `pointer-events:auto` 를 줘도 박스가 위에서 클릭을 가져간다 · `.wrap.ovp .bx{pointer-events:none}` 로 「페이지」일 때만 박스를 뺀다 · N-얹기 d · N-도크재편
- **부모 문서의 끌기가 판 위에서 끊긴다.** 포인터 이벤트는 **프레임 경계를 안 넘는다** — 자에서 끌어 내리다 커서가 iframe 위로 들어가는 순간 부모 `document` 의 `pointermove` · `pointerup` 이 통째로 안 온다(실측 · move 0 · up 0 · 선이 자 밑에 얼어붙었다) · N-자. `setPointerDown` 대상에 `setPointerCapture` 를 걸면 넘어온다. **잡는 대상은 안 사라지는 것이어야 한다** — 끄는 중에 그 선을 DOM 에서 지우면 캡처가 풀려 다시 끊긴다 · 그래서 끄는 선도 계속 그려 두고 자리만 갈아 끼운다
- **자석 자리는 화면 px 로 잰다** · N-자. 판 px 로 10 을 박아 두면 **축척 30% 에서 화면 3px** 이 되어 손이 못 맞춘다 · 배율마다 손맛이 달라진다. `9 / 축척` 으로 판 px 을 낸다 — 축척은 판이 뜰 때 클로저에 갇히므로 `축척ref` 로 읽는다
- **자석이 통째로 안 먹었다.** `붙이기()` 가 못 붙었을 때 **제자리를 돌려줬다** · 그 뒤 시작 모서리와 끝 모서리 중 「덜 움직인 쪽」을 골랐는데 · **안 붙은 쪽은 언제나 0px 라 진짜로 붙은 쪽이 늘 졌다**. 실측 · 박스 모서리 6px 앞에서 Shift 로 놓아도 636 에 안 붙고 630 에 그대로 앉았다 · 사용자 지적 · N-도크재편 c. 못 붙으면 `null` 을 내게 하고 · **붙은 것들 중에서만** 가까운 쪽을 고른다. 「안 붙었다」와 「제자리에 붙었다」는 다른 값이다
- **선은 굵기가 곧 클릭 영역이다.** 2px 선이 축척 30% 에서 화면 0.6px 이 되어 **드래그도 클릭도 통째로 안 먹었다** · 사용자 지적 · N-얹기 c. 기계는 맞았고 과녁이 없었다 — `.wrap.ovp .ov:before` 로 사방 14px 투명 테를 둘러 30px 과녁을 만든다. `::before` 는 제 부모가 눌린 것으로 잡혀 `closest('[data-얹기]')` 가 그대로 먹는다. **보이는 것은 안 바뀐다**
- **걸러 내는 열쇠는 거르기 전에 검사한다.** `얹기.층` 을 거른 뒤에 봤더니 모르는 값(`"가운데"`)이 앞에도 뒤에도 안 걸려 **조용히 사라졌다** — 문안에 적었는데 판에 안 나오고 이유를 못 댄다 · N-얹기 b
- **색 고르는 법이 도크 안에서 둘이었다.** 도형 배경 · 테두리는 견본 + 최근색 + `#RRGGBB` 인데 글자 색만 이름 칩 여섯이었다 · 사용자 지적 · N-글자 e. 배경은 이름이 하나(블록배경)뿐인데 아무 색이나 됐고 · 글자는 이름 여섯인데 아무 색도 안 됐다. **도형 꼴로 맞췄다**
- **클래스로는 `#E68100` 을 못 쓴다.** 선택자에 안 들어간다 · 그래서 이름은 클래스 · `#` 은 인라인 style 로 갈린다. 되읽기가 그 둘을 합쳐 순서까지 맞추려 들면 흐려져서 **표기를 `data-i` 에 그대로 싣고 `원문()` 이 그것만 읽는다**
- **`b{color:var(--강조)}` 가 굵게에 색을 묶어 둔다.** 구간 색이 `{결론|**38억**}` 에서 지려 한다 · `page.css ㊱` 이 `.bx .i-결론 b` 로 그 하나를 이긴다
- **도형 판이 셋으로 갈려 있었다.** 박스 탭 · 요소 탭 · 얹기 도형이 같은 어휘 일곱을 각자 적고 있어 한 곳을 고치면 나머지 둘이 뒤처졌다 — 실제로 갈려 있었다 · **박스에만 테두리 최근색이 있었다**. `도형판` 한 부품으로 묶었다 · N-도크재편. **얹기에는 「글자」를 안 낸다** — 렌더러가 얹은 것에 `data-글자` 를 안 붙여 문안에만 남는 죽은 열쇠가 된다
- **얹기 「글」도 `도형` 을 받는다** · `얹기하나()` 가 글 · 그림 · 도형 셋 다 `도형()` 을 부른다. 다만 반전은 `data-글자` 로 가는데 얹은 것에는 그 표식이 안 붙는다 — 배경을 깔고 글자를 뒤집는 길이 얹는 층에는 아직 없다
- 챗 쪽 VM 에는 playwright 브라우저가 없다 · `roundtrip` · `박스재기` · `pdf` 는 사용자 Mac 에서만 돈다 (**08-31 세션은 사용자 Mac 에서 돌았다** · 관문 전부 실행함)

---

## 6. 다음 세션 착수점

**N-도크재편은 닫혔다** · 08-31. 지은 것 · 잰 것 · 안 한 것이 아래에 있다.

### 6-0. N-도크재편이 낸 것

```
탭 넷 → 둘        [판](판면 + 박스) · [내용](층 목록 + 놓기 + 고른 것)
머리띠 걷기        `.dkhd` · `.dkln` 폐기 · 세 값이 제자리로 · §3
층 목록           앞 무리 · 박스 칸막이(고르면 요소 목록이 펼쳐진다) · 뒤 무리
삽입 스위치        놓기와 얹기의 유일한 차이 · 갈래 · 고른 것 판 · 판 클릭까지 정한다
                  값 둘이 데이터를 그대로 가리킨다 · 박스(`박스.내용[]`) · 페이지(`페이지.얹기[]`)
도형판 한 부품     박스 · 요소 · 얹기가 같은 어휘 일곱을 셋으로 적던 것을 묶었다
라벨              층→레이어 · 놓기→삽입 · 박스 안→박스 · 판 위→페이지 · `~하기` 를 안 쓴다
Shift 자석         **안 먹던 것을 고쳤다** · §5 · 붙은 것 중에서만 고른다 · 자리는 화면 px 로 잰다
자 · 안내선        §3-1 · 자에서 끌어 내린다 · 자석 후보에 사람이 세운 선이 든다
                  `문서.안내선` · 문서 하나 · 렌더러가 안 읽는다 · 판 밖에 그린다
색                박스 계열 주황 · 페이지 계열 보라 · 안내선 청록 · §3-1
데이터            **한 자도 안 바꿨다** · 층('앞'|'뒤') + 배열 순서 그대로다
```

**관문 넷이 한 칸도 안 달라졌다** · 도크만 고쳤다는 증거다.
`기준선 0/205` · `박스재기 7/205` · `원고검사 --봉인본 관문 여섯 통과` · `예산 78행`.

### 6-1. 잰 것 · 도크 세로

머리 없이 띄워 쟀다 · `node _DEV/scratch/도크시험.mjs` · 창 1600 × 1000.

```
도크 머리        0px     `.dkhd`(46) · `.dkln`(내용 목록 + 놓기 · 약 490)이 다 사라졌다
[판]   본문 900 · 내용 1653     판면 이름 + 카피 영역 + 격자 열둘 + 박스 + 도형 일곱
[내용] 본문 900 · 내용 2051     층 목록 317 + 놓기 196 + 고른 것
```

**스크롤 자체는 늘었는데 보이는 창이 두 배 넘게 커졌다.** 옛 도크는 내용 목록과
놓기 칩이 탭 위 고정 자리(`.dkln`)에 앉아 스크롤 창을 900 → 약 368 로 깎고 있었다.
지금은 그것이 스크롤 안으로 들어와 **한 번에 보이는 줄 수가 늘었다.**
`[내용]` 이 2036 인 것은 층 목록 · 놓기 · 요소 판 · 구간 판 · 도형 판이 한 탭에 다
있어서다 — 갈래별 판이 접히지 않는 한 줄어들 자리가 아니다.

### 6-2. 손짓 · 되나

- **칸막이 넘기기 된다** · 맨 아래 것을 「앞으로」 세 번 밀어 뒤 → 앞 → 앞 무리 맨 위까지
  올라가는 것을 확인했다(`도크시험.mjs`). 끌기(drag)는 안 넣었다 — `↑↓` 버튼이다
- **판이 누구를 받는지 스위치가 바로 바꾼다** · 「박스 안」으로 돌리면 `.ovp` 가 꺼지고
  판이 다시 박스를 받는다 · 확인함
- **겹친 것을 눈으로 가릴 수 있나 · 반만 된다.** 얹은 것끼리는 층 목록이 순서를 말해
  가릴 수 있다 — 셋을 같은 자리(113,401)에 포개 놓고 확인했다. 다만 **판에서는 위의
  것만 잡히고** · **얹은 것과 박스 안 글이 겹치는 것은 목록이 아예 못 본다**(박스가
  통째로 한 칸이라 그 안까지 못 들어간다). **「겹침 N」이 필요하다** · §7 첫 줄

### 6-3. 라벨 · 안 한 것 둘

사용자가 넘긴 표는 아직 안 왔다. 앞 세션이 반영한 셋(`0b8e6f5` 둘 · `7c852e2` 하나)은
그대로 살아 있고 · 프롬프트 §2-3 이 「아직 안 정한 것」이라 부른 둘은 **안 정했다.**

```
얹기 갈래 「도형」이 두 뜻       + 도형 으로 놓는 사각형  vs  배경 · 테두리 · 모서리 · 그림자
                             이름을 갈라야 한다 · **사용자 판정을 기다린다**
얹기에 「글자」가 없다           이번에 안 넣었다 · 렌더러가 얹은 것에 data-글자 를 안 붙인다
                             넣으면 문안에만 남는 죽은 열쇠가 된다 · **렌더러를 고칠 일이다**
도형 일곱을 한 부품으로        했다 · `도형판` · 박스 · 요소 · 얹기가 같은 것을 쓴다
```

**T 계열은 닫혔다** · 08-31.

```
T0  scripts/예산.mjs      빈 박스에 몇 줄 드는지 78행 · --json · --규격
T1  _DEV/원고규격서.md      원고 쓰기 전에 읽는 고정 문서 · 4판
    scripts/원고검사.mjs    문법 · 계약 · 예산 · 형태 넷을 묻는다 · --봉인본 이 관문을 교정한다
T3  변환기                 **안 짓는다** · 사용자 판정 · 편집기로만 간다
```

### 6-4. 다음에 남는 것 · **여기가 착수점이다**

```
겹침 검사      얹은 것과 박스 안 글이 겹치는지 · 상단바 「넘침 N」 옆에 「겹침 N」
              §6-2 가 이것이 필요하다고 판정했다 · 레이어 목록으로는 못 보는 겹침이다
자리 맞추기     고른 얹은 것 하나를 고른 박스(또는 판)에 딱 붙인다 · [왼쪽][가운데][오른쪽] …
              **다중 선택이 없어도 된다** · `얹을자리()` 가 이미 안쪽 사각형을 안다
              여럿 정렬 · 분배는 다중 선택이 먼저다 · 그 다음이다
판면 구성      page.구성 으로 칸을 자유롭게 놓는 길 · 지금은 띠가 하나 또는 둘로 막혀 있다
라벨 둘        얹기 「도형」 이름 가르기 · 얹기 「글자」 · §6-3 · 사용자 판정을 기다린다
T2 판면 템플릿  자주 쓰는 판면을 견본으로 · 판면을 사람이 정하는 길이 되었으니 값이 커졌다
원고 규격서     T3 을 접었으니 「챗이 칸을 채우는 법」으로 다시 좁힐지 판단한다
```

**`_DEV/prompt/N-도크재편_레이어_구현프롬프트.md`** 에 이번 판정의 근거가 다 있다.

아래 §6 나머지는 T 계열이 열려 있던 때의 기록이다 · 실측값은 그대로 쓴다.
**T3 변환기는 접었다** · 사용자 판정 · 편집기로만 간다.

---

### (아래는 T 계열 기록)

> **도구는 다 지었다.** `N-그림` · `N-자유` · `N-글자` · `N-얹기` 로 계획된 기능이 전부 닫혔다.
> 남은 것은 **문안을 만드는 길**이었다.

```
원고 → [ 여기가 비어 있다 ] → 페이지 JSON → 렌더러 → 지면
```

### 정본이 어디인가 · **먼저 읽을 것**

```
_DEV/prompt/T_원고입력층_설계검토요청.md    원고 입력층의 정본이다 · 이유 · 방법 · 묻는 것 아홉
HANDOFF.md (이 문서)                     그 밖 전부의 정본이다
```

**T 문서가 레포 상태를 적은 대목은 이 문서가 이긴다.** 그 문서는 얹는 층이 서기 전에 썼다 —
`§1-3` 이 「얹는 층이냐 T1 이냐」로 순서를 다투는데 **얹는 층은 이미 닫혔다**(`N-얹기 a~d`).
그 다툼은 끝났고 · 남은 물음 여덟은 그대로 살아 있다.

---

### 무엇을 지으려는가

```
[ T1 규격서 ] + [ 문체 기준집 ] + [ 건별 지시서 ]
        ↓ 클로드가 읽는다
   원고 마크다운 (.md)
        ↓ T3 변환기
   페이지 JSON
        ↓ 편집기에서 사람이 고친다
   렌더러 → HTML → PDF
```

| | 무엇 | 상태 |
|---|---|---|
| **T1** | 원고 규격서 · 문법 · 레이아웃 고르는 기준 · 박스별 예산 · 표기 규칙 | 없다 · **먼저** |
| **T3** | 변환기 · 마크다운 → 페이지 JSON | 없다 · T1 다음 |
| **T2** | 템플릿 · 제안서 1종 | 없다 · **뒤로 미뤘다** · 아래 실측이 이걸 흔든다 |

**사용자 확정** · 제안서 하나만 · 표본은 `content/sokcho/실행계획서.json` 18쪽 ·
문체는 앱 밖(`work-rules` 스킬) · 순서 T1 → T3 → N-이관.

---

### 실측 · **원고만으로 39쪽이 재현되나** · T 문서 §6-4 의 답

그 문서가 「제일 중요한 물음」이라 부른 것이다. 봉인본 셋을 전수로 셌다.

```
표 50건
  열 폭을 준 표      44건 (88%)   전부 백분율 꼴
    그중 균등        14건         자동으로 채울 수 있다
    **불균등        30건 (60%)**  13% · 11% · 42% · 34% 같은 값
  정렬을 쓴 표       19건 (38%)

블록 157개
  배경(도형)        39개 (24%)
  라벨              147개 (93%)   → 새 체계의 「제목」
내용 조각 201개      목록 77 · 표 50 · 문단 45 · 위여백 26 · 번호목록 12 · 단계띠 7 · 수치 4 · 그 밖 6
```

**판정 · 원고만으로는 안 나온다.** 표의 60% 가 **불균등 열 폭**을 들고 있고 그 값은
칸 안 글자 길이를 보고 사람이 그 자리에서 정한 것이다. 마크다운 표가 못 나른다.

그래서 분담이 이렇게 갈린다 · **T 문서 §3 의 「원고는 조판 열쇠를 못 준다」는 지키되**

```
원고가 내는 것    페이지 나눔 · 레이아웃 · 박스 나눔 · 글 · 표의 값 · 목록
편집기가 받는 것   표 열 폭 · 정렬 · 도형 · 얹기 · 그림 크기 · 크기 계단
```

**T2 를 앞으로 당길 근거가 여기 있다.** 불균등 폭 30건이 반복되는 꼴이면 템플릿이 받고 ·
아니면 편집기가 건건이 받는다. **N-이관 첫 몇 쪽을 옮겨 보고 정한다.**

---

### 아직 안 정한 것 · T 문서 §6 에서 살아 있는 물음

| | 물음 | 어디 |
|---|---|---|
| ② | 문단 · 목록의 줄 수를 어떻게 세나 · `확정높이()` 가 `null` 을 준다 | 예산 계산기가 여기 걸린다 |
| ③ | 마크다운이 못 가르는 자리 · 제목 vs 요약 · 수치 표기 · 빈칸의 42 블록 수 | 표기 초안 §4-2 |
| ⑤ | 왕복이냐 단방향이냐 · JSON → 마크다운 역변환이 값을 하나 | `roundtrip.mjs` 와 다른 물건이다 |
| ⑥ | 변환기 자리 · `scripts/` `render/` `app/api/` 중 어디 · `render/` 는 node 의존 0 규약이 있다 | |
| ⑦ | 검증 주체 · 변환기가 렌더러 계약을 다시 구현하면 이중 관리다 | |

**②가 T1 의 목줄이다.** 규격서가 「이 박스에 몇 줄」을 못 적으면 클로드가 분량을 못 맞춘다.
`scripts/예산.mjs` 를 만들어 §2-1 표에서 뽑는 것이 T 문서의 제안이다 · `§4-1`.

---

### 착수 전에 알아 둘 것

- **§2-1 레이아웃 표가 예산의 바탕이다.** 손으로 옮겨 적지 마라 — 코드가 바뀌면 거짓말이 된다
- **N-이관이 T3 의 첫 실전 시험이다.** 39쪽을 옮기다 막히는 자리가 곧 변환기의 구멍이다
- **표 50건이 거기서 실전에 오른다** · 9열 표 1건은 예외로 남겨 뒀다
- **봉인본 문안 넷은 옛 12칸 트랙이다** · 새 렌더러가 못 읽는다 · 값만 옮겨 온다

---

## 7. 미뤄 둔 것 · 알고 있는 구멍

- **오류판에 떨어지면 편집기에서 못 고친다.** 배열이 되며 「그 요소만 비워 그리기」가 값싸졌다 · 렌더러가 던지면 페이지 전체가 오류판이 되어 박스를 못 고른다 → JSON 을 손으로 고쳐야 한다. 「문안을 열 때 검사해 그 박스만 비워 그리기」가 값싼 개선안이다
- **값 하나 바꿀 때마다 판 iframe 이 통째로 새로 뜬다.** 판이 한 번 깜빡인다 · 다시 안 그리고 DOM 만 손대는 길을 따로 내야 한다
- **페이지가 많은 문안에서 미리보기 로딩**은 아직 눈으로 확인 안 됐다. 무거우면 썸네일 자체 캐시로 바꾼다
- **N-이관** · 실물 39쪽을 새 체계로 옮기는 일이 통째로 남아 있다. 표 50건이 여기서 실전에 오른다 · **T3 변환기의 첫 실전 시험이다** · §6
- **`assets/올린것/Unknown_복사본.png`** · 끌어다 놓기를 시험하며 올린 2MB 짜리다. 커밋에 안 넣었다 — 쓸 것이면 이름을 고치고 · 아니면 지운다
- **도크에 눈 검사가 안 든 곳이 남아 있다.** 08-31 세션이 넷을 코드로 잡았고 · **N-도크재편은 머리 없이 눌러 봤다**(`_DEV/scratch/도크시험.mjs` · 탭 둘 · 층 목록 · 칸막이 넘기기 · 스위치 · 도형판 셋). **사람 눈으로 본 것은 아니다** — 탭 둘의 세로 배치와 층 목록 꼴을 아직 아무도 안 봤다
- **층 목록에 끌기(drag)가 없다.** `↑↓` 버튼으로만 오르내린다 · 줄이 많아지면 한 칸씩 미는 것이 지친다. 넣는다면 칸막이를 넘는 것까지 한 손짓으로 가야 한다
- **얹기 「도형」이 두 뜻이다.** 갈래 이름(`+ 도형` 으로 놓는 사각형)과 꼴 어휘(배경 · 테두리 · 모서리 · 그림자)가 같은 말이다 · 얹기에서만 겹친다 · §6-3 · **사용자 판정을 기다린다**
- **굵게 버튼은 판면에서 글자를 연 뒤에만 듣는다.** 안 열었으면 로그로 말하고 만다. 고른 글자가 없으면 execCommand 가 커서 자리에만 걸린다 — 그 뒤 타이핑이 굵어진다
- 9열 표 1건은 예외로 남겨 뒀다
- **`assets/` 가 git 에 들어간다.** 끌어다 놓을 때마다 레포가 커진다 · 실물 39쪽어치 캡처면 무시 못 할 값이다. `assets/올린것/` 을 따로 둔 이유가 이것이다 — 나중에 통째로 다룰 수 있게

---

## 8. 📁 구조

```
rules/page.css      판면 규칙. 끝에만 덧붙인다 (… ㉟ 글자 크기 · ㊱ 구간 스타일 · ㊲ 얹는 층 · @media print 는 맨 끝)
                    `.sp`(단계띠)는 아무도 안 부르는 죽은 규칙이다 · 규칙을 통째로 손볼 때 걷는다
content/            문안의 유일 원본  sokcho/ · gangneung/ · _check/
render/             순수 함수 렌더러 (index.js · inline.js) · node 의존 0
                    inline.js 가 표기와 되읽기를 **둘 다** 갖는다 · inline() ↔ 원문() 은 역함수다
scripts/            build · pdf · roundtrip · 기준선 · 박스재기 · 비움 · save.sh · fonts.sh
app/                Next 도구 (dev 전용)
  api/css · api/font · api/doc
  api/img           `/api/img` 목록 · `/api/img/<경로>` 파일 하나
  ui/Shell.jsx      도구 전부 · 미리보기 · 제자리 편집 · 속성 도크
  lib/docs.js       content 접근
_DEV/               이관 문서 · 설계 · 프롬프트
  00_설계_나인프레스_v2.md          설계 정본 · §4-4 세로 스냅 · §5 실측 · §8 열린 물음
  prompt/T_원고입력층_…요청.md      **원고 입력층 정본** · 다음 세션이 §6 다음으로 읽는다
_legacy/            규칙 이전 손조판본
```
