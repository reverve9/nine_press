# 🔄 HANDOFF — nine_press

> 마지막 업데이트: 2026-08-18 · 세션: claude.ai (챗)
> 현재 Phase: P1 확정 / 다음은 P2

## 📍 현재 상태

판면 규칙 **v4 (①~⑰) 확정**. 실행계획서 **P1 여덟 면(01~08) 확정** — 전 면 넘침 0 · 여유 29px.
도구는 **판면에서 제자리 편집 + 구조 편집**까지 들어왔다.
**문구 다듬기는 이제 사용자가 도구로 직접 한다.**

## ✅ 완료

- **판면 규칙 v4** — `rules/page.css` 끝 ①~⑰
  - ①~⑤ §7 판정(볼드 낮춤 · 표 숫자 강조 해제 · 묶음 이름 보통 · 라벨 검정 · `.bt` 폐기)
  - ⑥ 인쇄 경로 교정 — `@media print` 뒤 무조건 규칙이 덮어 A4 좌상단에만 찍히던 결함
  - ⑦ 실무 확인 띠 통일 · ⑧ 헤더 대시 −9.2u
  - ⑨⑩ 인쇄 농도(헤더 C `#BFD5D3` / 띠 D `#DADADA`) · 글자 먹
  - ⑪ 번호목록 배지 복구 · ⑫ `.row.g` 에서 폭 2·3 복구 · ⑭ 배지 12u·중심 맞춤 — 전부 버그
  - ⑬ 표 `fit` + 밀도 `md` · ⑮ 목록 행 간격 60px
  - ⑯ 블록 테두리 `#ADADAD` · ⑰ 표 구분선 `#D8D8D8` / 머리 `#BFBFBF`
- **문안** `content/sokcho/실행계획서.json` — 01 전체 타임테이블 / 02 접수·심사·선정 /
  03 유형 배분 / 04 사전분석 / 05 진단 체계 / 06 회차 구성 / 07 회차 운영 상세 / 08 회차 간 밀착 지원
  - 어휘·문체 전수 정리 — 서술형 종결 0건, 설명체 라벨 0건, 면 참조는 `p.NN` 통일
- **도구**
  - **판면 제자리 편집** — 글자를 눌러 그 자리에 타이핑. `⌘B` 굵게 · Enter 줄바꿈 ·
    Esc 되돌림 · `⌘S` 저장. 고친 DOM 을 원문 표기로 되돌려 저장한다
  - **구조 편집** — 면 추가·복제·삭제·순서(번호 자동) · 행·열·블록 추가·삭제 ·
    열 폭 1/2/3 · 블록 배경 · 내용 덩이(목록/문단/표) 추가·삭제
  - **되돌리기** `⌘Z` / 다시 `⌘⇧Z` (60단계)
  - **넘침 표시** — 저장하면 사이드패널 아래에 「넘침 없음 · 여유 29px」
  - 도구 표식(`data-p`·`data-b`)은 미리보기에만. 산출 HTML 은 그대로
- **검증** — `scripts/roundtrip.mjs` 원문 왕복 **507개 전수 일치**
- **PDF** — 2340×1654 → A4 가로 297×210mm 벡터, 폰트 서브셋 내장
- **`_legacy/`** — `sclocalfood/_DEV/_docu/` 의 손조판 04·05면·판면견본·PNG. 규칙 v2 의 출처
- **git** 최초 커밋 `dace9e1`

## 🔧 진행 중 (미완료)

- **도구 실물 확인 안 됨** — 챗은 Mac 의 `localhost:4400` 에 못 붙는다. dev 서버 재시작 후 확인
- **인쇄 재확인** — 농도·테두리·표선을 올린 뒤 아직 안 뽑았다

## 📋 TODO

1. 도구 실물 확인 — 글자 클릭 → 타이핑 → `⌘Z` → `⌘S` → 넘침 표시
2. **문구 다듬기 — 사용자가 도구로 직접**
3. P2 일곱 면 — 09 전문가 운영 / 10 사례 공유회 / 11 종료 후 자가관리 /
   16 홍보·아카이빙 / 17 보고 체계와 성과품 / 18 투입 인력 / 19 리스크 대응
4. P3 네 면 12~15 — **플랫폼 화면 캡처가 나온 뒤**
5. 별첨 서식 9종 — 참여 신청서·동의서는 초안 있음. **여섯 종은 새로 설계**

## ⚠️ 이슈 / 주의사항

- **`device_bash` 로 git 을 돌리지 마라** — 커밋은 되지만 `.git/*.lock` 과 `tmp_obj_*` 를
  지우지 못해(삭제 권한 없음) 다음 git 명령이 막힌다. **`sh scripts/save.sh "메시지"`** 를 쓴다
- **저장 충돌** — 화면 저장은 파일 수정 시각을 대조한다. 챗 세션이 같은 JSON 을 고친 뒤라면
  저장이 거부된다. 문서를 다시 고르면 최신본을 불러온다
- **면의 마지막 행에는 `g` 가 붙는다** — 폭 2·3 은 ⑫ 이후 살아 있지만 새 구성은 확인할 것
- 규칙이나 인라인 표기를 고치면 **`node scripts/roundtrip.mjs`** 를 돌린다.
  어긋나면 제자리 편집이 문안을 깨뜨린다는 뜻

## 📁 구조

```
rules/page.css      판면 규칙 (v2 → v3 → v4 순서로 덧쓴다. 앞 블록은 고치지 않는다)
content/            문안의 유일 원본
render/             순수 함수 렌더러 (index.js · inline.js)
scripts/            build · pdf · shot · roundtrip · save.sh · fonts.sh
app/                Next 도구 (dev 전용. next build 를 돌리지 않는다)
_legacy/            규칙 이전 손조판본. 참고용
_DEV/_ref/          단계별 백업 (v3_backup · Shell_form_v1/v2 · render_before_inplace)
```

## 💡 메모

- **행간** — 남는 높이를 끝까지 분배(안 B)하면 항목 서넛짜리 블록에서 흩어져 안 읽힌다.
  넉넉한 고정 행간 + 하단 여백(안 C)으로 확정
- **배지 정렬** — 위치가 아니라 크기 문제였다. 배지 27.3px / 글자 잉크 21px 이라 중심을
  맞춰도 아래가 무겁다. 배지를 12u 로 줄여 해결
- **저장은 통째로 쓴다** — patch 를 만들지 않는다. 수정 시각 대조가 충돌을 막는다
- **어휘** — 설명체 라벨(「보는 것」 「남는 것」)은 쓰지 않는다. 명사구로 끝낸다
